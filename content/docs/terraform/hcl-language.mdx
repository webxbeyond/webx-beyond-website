---
title: টেরাফর্ম কনফিগারেশন ল্যাঙ্গুয়েজ (HCL)
icon: ph:code-duotone
---

Terraform-এর কনফিগারেশন লেখার জন্য HashiCorp Configuration Language (HCL) ব্যবহার করা হয়। এটি সহজ, মানব-বান্ধব এবং ডিক্লারেটিভ। এই টিউটোরিয়ালে HCL-এর সিনট্যাক্স, ব্লক, আর্গুমেন্ট, এক্সপ্রেশন—সবকিছু হাতে-কলমে উদাহরণসহ দেখানো হবে।

---

## HCL সিনট্যাক্স

- **ফাইল এক্সটেনশন:** `.tf` (যেমন: `main.tf`)
- **কেস:** কেস-ইনসেনসিটিভ (resource, RESOURCE, Resource—সবই চলে)
- **কমেন্ট:**
  - এক লাইনের জন্য: `#` বা `//`
  - মাল্টিলাইন: `/* ... */`

**উদাহরণ:**
```hcl
# এটা এক লাইনের কমেন্ট
// এটাও
/*
এটা
মাল্টিলাইন
কমেন্ট
*/
```

---

## ব্লক (Block)

HCL-এর মূল গঠন হল ব্লক। প্রতিটি ব্লক `{ ... }` দিয়ে ঘেরা।

**বেসিক ব্লক স্ট্রাকচার:**
```hcl
block_type "label1" "label2" {
  argument1 = value1
  argument2 = value2
}
```

- **block_type:** যেমন `resource`, `provider`, `variable`
- **label:** ব্লকের নাম বা টাইপ চিহ্নিত করে
- **body:** আর্গুমেন্ট ও সাব-ব্লক

**উদাহরণ:**
```hcl
resource "aws_instance" "example" {
  ami           = "ami-123456"
  instance_type = "t2.micro"
}
```

---

## আর্গুমেন্ট (Argument)

প্রতিটি ব্লকের ভেতরে কী-ভ্যালু পেয়ার থাকে, যেগুলোকে আর্গুমেন্ট বলে।

**উদাহরণ:**
```hcl
provider "aws" {
  region = "us-east-1"
}
```

এখানে `region` হল আর্গুমেন্ট, যার ভ্যালু `"us-east-1"`।

---

## এক্সপ্রেশন (Expression)

এক্সপ্রেশন দিয়ে ভ্যালু ডাইনামিকভাবে সেট করা যায়।

- **লিটারেল:** সরাসরি ভ্যালু (string, number, bool)
- **রেফারেন্স:** অন্য রিসোর্স/ভ্যারিয়েবলকে রেফার করা
- **ফাংশন:** বিল্ট-ইন ফাংশন ব্যবহার
- **ইন্টারপোলেশন:** `${ ... }` দিয়ে এক্সপ্রেশন লেখা

**উদাহরণ:**
```hcl
output "instance_id" {
  value = aws_instance.example.id
}

variable "env" {
  default = "dev"
}

resource "aws_s3_bucket" "bucket" {
  bucket = "my-bucket-${var.env}"
}
```

---

## হাতে-কলমে: একটি সম্পূর্ণ কনফিগারেশন

```hcl
provider "aws" {
  region = "us-east-1"
}

resource "aws_instance" "web" {
  ami           = "ami-123456"
  instance_type = "t2.micro"
  tags = {
    Name = "MyWebServer"
  }
}

output "instance_public_ip" {
  value = aws_instance.web.public_ip
}
```

- এখানে provider, resource, output—সব ব্লক দেখানো হয়েছে
- আর্গুমেন্ট, এক্সপ্রেশন, ইন্টারপোলেশন—সব ব্যবহার হয়েছে

---

## টিপস ও বেস্ট প্র্যাকটিস

- কোডে কমেন্ট দিন—বোঝা সহজ হবে
- ইনডেন্টেশন ও স্পেসিং ঠিক রাখুন
- ভ্যারিয়েবল ও আউটপুট ব্যবহার করুন—কনফিগ ডাইনামিক ও রিইউজেবল হবে
- ছোট ছোট ব্লকে কোড ভাগ করুন

---

## সমস্যা ও সমাধান

**সমস্যা:** কনফিগারেশনে টাইপো বা ভুল সিনট্যাক্স
- **সমাধান:** `terraform validate` কমান্ড চালান, ভুল দেখাবে

**সমস্যা:** রিসোর্স রেফারেন্স ভুল
- **সমাধান:** রিসোর্সের নাম ও লেবেল ঠিক আছে কিনা চেক করুন

---

## আরও জানুন
- [Terraform Configuration Language Docs](https://developer.hashicorp.com/terraform/language)
- [HCL Syntax Guide](https://github.com/hashicorp/hcl)

এই টিউটোরিয়াল পড়ে আপনি HCL-এর বেসিক থেকে শুরু করে হাতে-কলমে কনফিগারেশন লিখতে পারবেন।
