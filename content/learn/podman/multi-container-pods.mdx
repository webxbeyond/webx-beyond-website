---
title: "একটি পডে একাধিক কনটেইনার চালানো"
icon: "🔗"
description: "একটি পডে একাধিক কনটেইনার চালানোর নিয়ম, নেটওয়ার্ক শেয়ারিং এবং অভ্যন্তরীণ যোগাযোগ।"
keywords: ["Podman", "Pods", "মাল্টি-কনটেইনার", "কনটেইনার কমিউনিকেশন", "পডম্যান"]
---

## পরিচিতি

আগের লেসনে আমরা পড তৈরি ও ম্যানেজমেন্ট শিখেছি। এবার আমরা আরও গভীরভাবে দেখব **একটি পডে একাধিক কনটেইনার কিভাবে একসাথে চালানো যায়**।  

পডের মূল ধারণা হলো **একটি লজিক্যাল ইউনিটে একাধিক কনটেইনার একত্রে রাখা**, যাতে তারা নেটওয়ার্ক এবং স্টোরেজ শেয়ার করতে পারে।  

**অনালজি:**  
ধরে নিন একটি ছোট হাউস (পড)। হাউসের ভিতরে একাধিক রুম (কনটেইনার) আছে। প্রত্যেক রুম আলাদা কাজ করছে, কিন্তু হাউসের ভিতরের নেটওয়ার্ক, পানি এবং বিদ্যুৎ সবাই শেয়ার করছে।  

---

## ১. পডে একাধিক কনটেইনার যোগ করা

### উদাহরণ:

```bash
podman pod create --name multipod -p 8080:80
podman run -dt --pod multipod nginx
podman run -dt --pod multipod redis
podman run -dt --pod multipod alpine sleep 3600
````

**বিস্তারিত ব্যাখ্যা:**

* প্রথম লাইন → পড তৈরি।
* দ্বিতীয় লাইন → Nginx কনটেইনার যোগ।
* তৃতীয় লাইন → Redis কনটেইনার যোগ।
* চতুর্থ লাইন → একটি আলপাইন কনটেইনার যোগ যা ১ ঘণ্টা (3600 সেকেন্ড) চলবে।
* সব কনটেইনার একই নেটওয়ার্ক স্পেস শেয়ার করছে।

---

## ২. কনটেইনারগুলোর অভ্যন্তরীণ যোগাযোগ

একটি পডের সব কনটেইনার একই **IP ঠিকানা** শেয়ার করে। তাই তারা **localhost** দিয়ে একে অপরের সাথে যোগাযোগ করতে পারে।

উদাহরণ:

* Nginx কনটেইনার Redis-কে `localhost:6379` দিয়ে অ্যাক্সেস করতে পারে।
* আলপাইন কনটেইনার Nginx কে `localhost:80` দিয়ে ping করতে পারে।

**অনালজি:**
পডের ভিতরের কনটেইনারগুলোকে একই রুমের মানুষ মনে করুন। তারা সহজেই একে অপরের সাথে কথা বলতে পারে, কারণ তারা একই ঘরে আছে।

---

## ৩. মাল্টি-কনটেইনার পডের অবস্থা দেখা

```bash
podman pod ps
podman ps --pod
podman pod inspect multipod
```

* `podman pod ps` → পডের তালিকা।
* `podman ps --pod` → পডের সব কনটেইনারের অবস্থা।
* `podman pod inspect multipod` → বিস্তারিত তথ্য, IP, পোর্ট, কনটেইনার তালিকা।

---

## ৪. মাল্টি-কনটেইনার পড বন্ধ ও মুছে ফেলা

```bash
podman pod stop multipod
podman pod rm multipod
```

* পড বন্ধ করলে সব কনটেইনার বন্ধ হবে।
* পড মুছে ফেললে সব কনটেইনারও মুছে যাবে।

---

## ব্যবহারিক উদাহরণ

**চেষ্টা করুন:**

1. একটি পড তৈরি করুন নাম দিন `dev-multipod`।
2. এই পডে তিনটি কনটেইনার যোগ করুন:

   * Nginx → হোস্ট পোর্ট 8083–এ।
   * Redis → ডিফল্ট পোর্ট 6379।
   * একটি আলপাইন কনটেইনার যা ১ ঘণ্টা চলবে।
3. পডের অবস্থা চেক করুন।
4. Nginx কনটেইনার থেকে Redis কনটেইনারকে `localhost:6379` দিয়ে ping করার চেষ্টা করুন।
5. সব কনটেইনার বন্ধ ও পড মুছে ফেলুন।

**কমান্ড উদাহরণ:**

```bash
podman pod create --name dev-multipod -p 8083:80
podman run -dt --pod dev-multipod nginx
podman run -dt --pod dev-multipod redis
podman run -dt --pod dev-multipod alpine sleep 3600
podman ps --pod
podman exec -it <nginx-container-id> ping localhost
podman pod stop dev-multipod
podman pod rm dev-multipod
```

---

## উপসংহার

একটি পডে একাধিক কনটেইনার চালানো মানে **নেটওয়ার্ক এবং স্টোরেজ শেয়ারিংসহ একসাথে একাধিক সার্ভিস পরিচালনা** করা।

এই লেসনের পর আপনি:

* পডে একাধিক কনটেইনার যোগ করতে পারবেন।
* কনটেইনারদের অভ্যন্তরীণভাবে যোগাযোগ করাতে পারবেন।
* পড চালনা, পরীক্ষা এবং মুছে ফেলা শিখেছেন।
