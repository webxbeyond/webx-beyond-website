---
title: "পডম্যান পডস"
icon: "📦"
description: "পডম্যানের ইউনিক ফিচার পডস বোঝা ও ব্যবহার করা। কিভাবে পড তৈরি, পরিচালনা এবং একাধিক কনটেইনার একসাথে চলানো যায়।"
keywords: ["Podman", "Pods", "কনটেইনার", "পডম্যান টিউটোরিয়াল", "পড ব্যবহার"]
---

## পরিচিতি

আপনি যদি কনটেইনার প্রযুক্তি নিয়ে কাজ করেন, তবে `পড` (Pod) শব্দটি নিশ্চয়ই শুনেছেন। পড একটি ধারণা যা Kubernetes থেকে অনুপ্রাণিত। সহজভাবে বললে, একটি পড হলো এক বা একাধিক কনটেইনারের **লজিক্যাল গ্রুপ**, যা একই নেটওয়ার্ক স্পেস এবং স্টোরেজ শেয়ার করে।  

পডম্যান (Podman) এই ধারণাটি ব্যবহার করে কনটেইনারগুলো আরও সহজভাবে একসাথে চালাতে এবং পরিচালনা করতে সাহায্য করে।  

**কেন পড দরকার?**
- একাধিক কনটেইনারকে একত্রে গঠন করার জন্য।
- কনটেইনারের মধ্যে সহজ নেটওয়ার্ক শেয়ারিং এবং কমিউনিকেশন করার জন্য।
- বাস্তব জীবনের মাইক্রোসার্ভিস আর্কিটেকচারে সুবিধা পেতে।  

---

## পডের মূল বৈশিষ্ট্য

পডের মধ্যে থাকা কনটেইনারগুলো কিছু সাধারণ বৈশিষ্ট্য শেয়ার করে:

- **নেটওয়ার্ক শেয়ারিং:** পডের সব কনটেইনার একই IP ঠিকানা এবং পোর্ট স্পেস শেয়ার করে।  
- **স্টোরেজ ভলিউম:** পডের ভেতরের কনটেইনারগুলো একাধিক ভলিউম শেয়ার করতে পারে।  
- **লাইফসাইকেল:** পডের জীবনচক্র একত্রে পরিচালিত হয়; পড বন্ধ হলে সব কনটেইনারও বন্ধ হয়।  

**অনালজি:**  
পডকে ভাবুন একটি ছোট অফিসের মতো। অফিসের ভিতরে একাধিক কর্মী (কনটেইনার) কাজ করছে। তারা একই অফিসের নেটওয়ার্ক এবং স্টোরেজ শেয়ার করে, তবে প্রত্যেকে আলাদা কাজ করছে।  

---

## পড তৈরি ও পরিচালনা

পডম্যান ব্যবহার করে পড তৈরি করা খুব সহজ। 

### ১. পড তৈরি করা

```bash
podman pod create --name mypod -p 8080:80
````

**ব্যাখ্যা:**

* `--name mypod` → পডের নাম।
* `-p 8080:80` → হোস্ট পোর্ট 8080 কে পডের 80 পোর্টের সাথে ম্যাপ করা।

### ২. কনটেইনার পডে যোগ করা

```bash
podman run -dt --pod mypod nginx
podman run -dt --pod mypod redis
```

এভাবে `mypod` পডে দুইটি কনটেইনার যোগ হলো: একটি Nginx এবং একটি Redis। তারা একই নেটওয়ার্ক শেয়ার করবে।

### ৩. পডের অবস্থা দেখা

```bash
podman pod ps
```

এই কমান্ড দিয়ে আপনি পডের তালিকা এবং চলমান কনটেইনার দেখতে পারবেন।

### ৪. পড বন্ধ করা

```bash
podman pod stop mypod
podman pod rm mypod
```

---

## ব্যবহারিক উদাহরণ

**চেষ্টা করুন:**

1. একটি নতুন পড তৈরি করুন নাম দিন `testpod`।
2. এই পডে একটি Nginx কনটেইনার এবং একটি Redis কনটেইনার যোগ করুন।
3. নিশ্চিত করুন যে Nginx হোস্টের পোর্ট 8081–এ চলে।
4. পড চালু থাকাকালীন কনটেইনারগুলো পরীক্ষা করুন।

**পরীক্ষার কমান্ডগুলো:**

```bash
podman pod create --name testpod -p 8081:80
podman run -dt --pod testpod nginx
podman run -dt --pod testpod redis
podman pod ps
```

**চেকলিস্ট:**

* পডের নাম ঠিক আছে কি না।
* Nginx এবং Redis কনটেইনার একই পডে আছে কি না।
* হোস্ট পোর্ট এবং পডের পোর্ট ঠিকভাবে ম্যাপ হয়েছে কি না।

---

## উপসংহার

পডম্যান পড ব্যবহার করে একাধিক কনটেইনারকে সহজে একত্রে পরিচালনা করা যায়। এটি Kubernetes-এর অনুপ্রেরণায় তৈরি, কিন্তু Podman দিয়ে সরাসরি, daemonless এবং rootless মোডে কাজ করে।

পড ধারণা বোঝা হলে পরবর্তী ধাপে আপনি **মাল্টি-কনটেইনার অ্যাপ্লিকেশন** সহজেই ডিপ্লয় করতে পারবেন।