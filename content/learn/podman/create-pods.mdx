---
title: "পড তৈরি ও ম্যানেজমেন্ট"
icon: "🛠️"
description: "পডম্যান পড তৈরি, চালনা ও পরিচালনার বিস্তারিত নির্দেশিকা। একাধিক কনটেইনারকে সহজে একত্রে ব্যবস্থাপনা করা।"
keywords: ["Podman", "Pods", "পড তৈরি", "পড ম্যানেজমেন্ট", "কনটেইনার"]
---

## পরিচিতি

আগের লেসনে আমরা জানলাম পড (Pod) কী এবং কেন এটি গুরুত্বপূর্ণ। এবার আমরা শিখব কিভাবে **পড তৈরি, চালনা এবং পরিচালনা** করতে হয়।  

পডম্যান পড ব্যবহার করে একাধিক কনটেইনারকে একটি একক ইউনিটের মতো পরিচালনা করা যায়। এটি বাস্তব জীবনের মাইক্রোসার্ভিস আর্কিটেকচারে খুবই কাজে লাগে।  

**অনালজি:**  
পডকে ভাবুন একটি ছোট কারখানার মতো। কারখানার ভিতরে একাধিক মেশিন (কনটেইনার) একসাথে কাজ করছে, কিন্তু তারা একসাথে একটি লজিক্যাল ইউনিট।  

---

## ১. পড তৈরি করা

পড তৈরি করার জন্য `podman pod create` কমান্ড ব্যবহার করা হয়।  

```bash
podman pod create --name mypod -p 8080:80
````

**বিস্তারিত ব্যাখ্যা:**

* `--name mypod` → পডের নাম।
* `-p 8080:80` → হোস্টের 8080 পোর্টকে পডের 80 পোর্টের সাথে ম্যাপ করা।
* এই পডে পরে এক বা একাধিক কনটেইনার যোগ করা যাবে।

### ১.১ rootless বা root মোডে পড তৈরি

* Rootless: ব্যবহারকারীর অনুমতি ছাড়া পড তৈরি হয়।
* Root: সিস্টেম প্রশাসকের অনুমতি প্রয়োজন।

**Rootless উদাহরণ:**

```bash
podman pod create --name mypod --publish 8080:80
```

---

## ২. কনটেইনারকে পডে যোগ করা

পড তৈরি হওয়ার পর কনটেইনার যোগ করতে হয় `--pod` ফ্ল্যাগ ব্যবহার করে।

```bash
podman run -dt --pod mypod nginx
podman run -dt --pod mypod redis
```

**বিস্তারিত ব্যাখ্যা:**

* প্রথম কনটেইনার Nginx, দ্বিতীয়টি Redis।
* উভয়ই একই নেটওয়ার্ক স্পেস শেয়ার করছে।
* পডের ভেতরে যোগাযোগ সহজ।

---

## ৩. পডের অবস্থা দেখা ও পরিচালনা

### ৩.১ পডের তালিকা

```bash
podman pod ps
```

* চলমান পড দেখায়।
* `POD ID`, `NAME`, `STATUS`, `CREATED`, `INFRA ID`, `# OF CONTAINERS` তথ্য দেয়।

### ৩.২ পডের বিস্তারিত দেখা

```bash
podman pod inspect mypod
```

* নেটওয়ার্ক, IP, পোর্ট ম্যাপিং, এবং কনটেইনার তথ্য দেয়।

### ৩.৩ পড বন্ধ করা

```bash
podman pod stop mypod
```

* পডের সমস্ত কনটেইনারও বন্ধ হবে।

### ৩.৪ পড মুছে ফেলা

```bash
podman pod rm mypod
```

* পড এবং এর সমস্ত কনটেইনার পুরোপুরি ডিলিট হয়ে যাবে।

---

## ৪. পডের লগ দেখা

পডের সব কনটেইনারের লগ একত্রে দেখতে পারেন:

```bash
podman logs -l --since 10m
```

* `-l` → সর্বশেষ কনটেইনার।
* `--since 10m` → শেষ ১০ মিনিটের লগ।

---

## ব্যবহারিক উদাহরণ

**চেষ্টা করুন:**

1. একটি নতুন পড তৈরি করুন নাম দিন `devpod`।
2. এই পডে দুটি কনটেইনার যোগ করুন:

   * একটি Nginx → হোস্ট পোর্ট 8082–এ।
   * একটি Redis।
3. পডের অবস্থা চেক করুন।
4. পড বন্ধ এবং মুছে ফেলুন।

**কমান্ড উদাহরণ:**

```bash
podman pod create --name devpod -p 8082:80
podman run -dt --pod devpod nginx
podman run -dt --pod devpod redis
podman pod ps
podman pod stop devpod
podman pod rm devpod
```

**চেকলিস্ট:**

* পডের নাম ঠিক আছে কি না।
* কনটেইনারগুলো পডে যোগ হয়েছে কি না।
* পোর্ট ম্যাপিং ঠিক আছে কি না।

---

## উপসংহার

পড তৈরি ও পরিচালনা করা খুব সহজ। পডম্যানের সাহায্যে একাধিক কনটেইনারকে একই নেটওয়ার্ক এবং স্টোরেজ শেয়ার করে চালানো যায়।

পরবর্তী লেসনে আমরা দেখব **পডে ভলিউম শেয়ারিং এবং নেটওয়ার্ক কনফিগারেশন**, যা আরও শক্তিশালী মাল্টি-কনটেইনার অ্যাপ্লিকেশন তৈরিতে সাহায্য করবে।