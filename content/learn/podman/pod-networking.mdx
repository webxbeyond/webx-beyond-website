---
title: "পড নেটওয়ার্কিং"
icon: "🌐"
description: "পডম্যান পডের নেটওয়ার্কিং ব্যাখ্যা। কিভাবে পডের কনটেইনারগুলো একে অপরের সাথে এবং হোস্টের সাথে যোগাযোগ করে।"
keywords: ["Podman", "Pods", "নেটওয়ার্কিং", "মাল্টি-কনটেইনার", "পডম্যান"]
---

## পরিচিতি

পডম্যান পডে নেটওয়ার্কিং হলো একটি গুরুত্বপূর্ণ ধারণা। একাধিক কনটেইনারের মধ্যে এবং হোস্ট মেশিনের সাথে **যোগাযোগ** সহজভাবে করতে পডের নেটওয়ার্ক সঠিকভাবে বোঝা জরুরি।  

পডের নেটওয়ার্কিং Kubernetes-এর অনুকরণে তৈরি, কিন্তু Podman-এ এটি **daemonless এবং rootless** মোডে সরাসরি কাজ করে।  

**অনালজি:**  
পডকে ভাবুন একটি ছোট অফিস। অফিসের ভিতরে একাধিক কর্মী (কনটেইনার) আছে। সবাই একই Wi-Fi (নেটওয়ার্ক স্পেস) ব্যবহার করছে, তাই তারা সহজে একে অপরের সাথে কথা বলতে পারে।  

---

## ১. পড নেটওয়ার্ক টাইপ

Podman মূলত তিন ধরনের নেটওয়ার্কিং সমর্থন করে:

1. **পড-ভিত্তিক নেটওয়ার্ক (Default / Bridge)**  
   - পডের সব কনটেইনার একই **IP ঠিকানা** শেয়ার করে।  
   - এক কনটেইনার থেকে অন্য কনটেইনারে **localhost** ব্যবহার করে অ্যাক্সেস সম্ভব।  

2. **হোস্ট নেটওয়ার্কিং**  
   - পড সরাসরি হোস্ট মেশিনের নেটওয়ার্ক ব্যবহার করে।  
   - কম ল্যাটেন্সি, কিন্তু IP এবং পোর্টের কনফ্লিক্টের সম্ভাবনা আছে।  
   ```bash
   podman pod create --name hostpod --network host
    ```

3. **নেটওয়ার্ক কাস্টমাইজেশন**

   * ব্যবহারকারী চাইলে নিজস্ব কাস্টম নেটওয়ার্ক তৈরি করতে পারে।

   ```bash
   podman network create mynetwork
   podman pod create --name custompod --network mynetwork
   ```

---

## ২. কনটেইনার অভ্যন্তরীণ যোগাযোগ

একটি পডের সব কনটেইনার একই **IP ঠিকানা** শেয়ার করে। তাই তারা একে অপরকে `localhost` বা কনটেইনারের নাম দিয়ে অ্যাক্সেস করতে পারে।

উদাহরণ:

```bash
podman pod create --name netpod
podman run -dt --pod netpod nginx
podman run -dt --pod netpod redis
```

* Nginx থেকে Redis অ্যাক্সেস করতে হবে:

  ```bash
  redis-cli -h localhost
  ```
* কোনো অতিরিক্ত IP কনফিগারেশন প্রয়োজন নেই।

**অনালজি:**
একই অফিসের মানুষরা একই Wi-Fi ব্যবহার করছে → সহজে একে অপরকে কল করতে পারে।

---

## ৩. পড হোস্ট পোর্ট ম্যাপিং

পডের কনটেইনারগুলোকে হোস্টে অ্যাক্সেসযোগ্য করতে হলে **পোর্ট ফরওয়ার্ডিং** করতে হয়।

```bash
podman pod create --name webpod -p 8080:80
podman run -dt --pod webpod nginx
```

* হোস্টের 8080 পোর্ট → পডের 80 পোর্টের সাথে ম্যাপ।
* ব্রাউজারে `http://localhost:8080` → Nginx সার্ভিস অ্যাক্সেস।

---

## ৪. কনটেইনার-টু-কনটেইনার কমিউনিকেশন

পডে থাকা কনটেইনারগুলো একে অপরের সাথে সহজে যোগাযোগ করতে পারে।

**ধাপে ধাপে পরীক্ষা:**

```bash
podman pod create --name testnetpod
podman run -dt --pod testnetpod nginx
podman run -dt --pod testnetpod alpine sleep 3600
```

* আলপাইন কনটেইনার থেকে Nginx কনটেইনারকে ping করুন:

  ```bash
  podman exec -it <alpine-container-id> ping localhost
  ```

**ফলাফল:** পডের অভ্যন্তরীণ নেটওয়ার্ক কাজ করছে।

---

## ৫. মাল্টি-পড নেটওয়ার্কিং

* একাধিক পড আলাদা নেটওয়ার্ক স্পেস পায়।
* পডের কনটেইনার শুধুমাত্র **একই পডের ভিতরে** `localhost` ব্যবহার করতে পারে।
* পডের বাইরের কনটেইনার অ্যাক্সেস করতে হলে **পোর্ট ফরওয়ার্ডিং বা কাস্টম নেটওয়ার্ক** ব্যবহার করতে হয়।

```bash
podman pod create --name pod1 -p 8081:80
podman pod create --name pod2 -p 8082:80
```

* `pod1` এবং `pod2` একে অপরকে সরাসরি localhost দিয়ে অ্যাক্সেস করতে পারবে না।
* কাস্টম নেটওয়ার্ক ব্যবহার করলে তারা একে অপরের সাথে কথা বলতে পারে।

---

## ব্যবহারিক উদাহরণ

**চেষ্টা করুন:**

1. একটি পড তৈরি করুন `networkpod`।
2. পডে দুইটি কনটেইনার যোগ করুন: Nginx এবং Redis।
3. পোর্ট ফরওয়ার্ডিং করুন: হোস্টের 8084 → Nginx 80।
4. পডের ভিতরের কনটেইনার একে অপরকে `localhost` দিয়ে ping করুন।
5. পড বন্ধ ও মুছে ফেলুন।

**কমান্ড উদাহরণ:**

```bash
podman pod create --name networkpod -p 8084:80
podman run -dt --pod networkpod nginx
podman run -dt --pod networkpod redis
podman exec -it <redis-container-id> ping localhost
podman pod stop networkpod
podman pod rm networkpod
```

---

## উপসংহার

পড নেটওয়ার্কিং বোঝা হলে আপনি:

* একাধিক কনটেইনারকে অভ্যন্তরীণভাবে যোগাযোগ করাতে পারবেন।
* হোস্ট এবং পডের মধ্যে পোর্ট ফরওয়ার্ড করতে পারবেন।
* কাস্টম নেটওয়ার্ক ব্যবহার করে মাল্টি-পড কমিউনিকেশন সহজভাবে করতে পারবেন।