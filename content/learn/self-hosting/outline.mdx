---
title: আউটলাইন (Outline)
icon: ph:cube-duotone
---

## ভূমিকা

### Outline কী এবং কেন Self-Host করবেন
**Outline** একটি ওপেন-সোর্স নলেজ বেস প্ল্যাটফর্ম যা টিমের ডকুমেন্টেশন, উইকি ও জ্ঞান শেয়ারিংয়ের জন্য তৈরি। নিজের সার্ভারে **Outline** হোস্ট করলে আপনি পাবেন:

- **সম্পূর্ণ নিয়ন্ত্রণ** — ডেটা আপনার সার্ভারে থাকে।
- **গোপনীয়তা নিশ্চিত** — তৃতীয় পক্ষের ঝুঁকি থাকে না।
- **কাস্টমাইজেশন** — আপনার প্রয়োজন অনুযায়ী কনফিগার করা যায়।

### মূল ব্যবহার ক্ষেত্র

- টিম ডকুমেন্টেশন ম্যানেজমেন্ট
- উইকি বা নলেজবেস
- প্রজেক্ট নলেজ শেয়ারিং

---

## প্রয়োজনীয়তা

### হার্ডওয়্যার

- CPU: 2 cores (সামে)
- RAM: 2GB (সুপারিশ: 4GB)
- Storage: 10GB SSD (বা বেশিতে ভালো)

### সফটওয়্যার

- OS: Ubuntu 22.04+ বা সমতুল্য Linux
- Docker & Docker Compose (ডকার ভিত্তিক সেটআপের জন্য)
- Node.js (v18+) ও PostgreSQL (v14+) (Bare-metal সেটআপের জন্য)

### নেটওয়ার্ক বিবেচনা

- পোর্টস: 3000 (Outline), 5432 (Postgres), 6379 (Redis)
- Static IP বা DDNS (বহিরাগত অ্যাক্সেসের জন্য)
- ডোমেইন নাম (SSL-এর জন্য সুপারিশকৃত)

---

## ইনস্টলেশন

নিচে দুইটি সাধারণ পদ্ধতি দেয়া হলো: Bare-metal (Node.js) এবং Docker Compose।

### মেথড ১ — Bare-metal (Linux)

1. Node.js, PostgreSQL, Redis ইন্সটল করুন:

```bash
sudo apt update && sudo apt install -y nodejs npm postgresql redis
```

2. Outline সোর্স ক্লোন করুন ও ডিরেক্টরিতে যান:

```bash
git clone https://github.com/outline/outline.git
cd outline
```

3. `.env` ফাইল কনফিগার করুন:

```bash
cp .env.sample .env
nano .env
```

গুরুত্বপূর্ণ ভ্যারিয়েবলসমূহ:

```text title=".env"
DATABASE_URL=postgres://user:password@localhost:5432/outline
REDIS_URL=redis://localhost:6379
SECRET_KEY=আপনার_জেনারেটেড_কি
```

4. অ্যাপ ইন্সটল ও চালু করুন:

```bash
npm install
npm run build
npm start
```

---

### মেথড ২ — Docker Compose (সহজ)

`docker-compose.yml` ফাইল তৈরি করুন (নিচের উদাহরণটি কনফিগার করে নিন):

```yaml title="docker-compose.yml"
services:
  outline:
    image: outlinewiki/outline:latest
    depends_on:
      - postgres
      - redis
    environment:
      DATABASE_URL: postgres://outline:password@postgres:5432/outline
      REDIS_URL: redis://redis:6379
      SECRET_KEY: your-secret-key
      URL: https://yourdomain.com
    ports:
      - "3000:3000"
    restart: unless-stopped

  postgres:
    image: postgres:14
    environment:
      POSTGRES_USER: outline
      POSTGRES_PASSWORD: password
      POSTGRES_DB: outline
    volumes:
      - ./postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:alpine
    restart: unless-stopped
```

রান করুন:

```bash
docker-compose up -d
```

সহজ সেটআপ টুলস (ঐচ্ছিক): Portainer, CapRover ইত্যাদি।

---

## কনফিগারেশন

`.env` ফাইলে অন্তর্ভুক্ত করুন:

- `DATABASE_URL`
- `REDIS_URL`
- `SECRET_KEY` (নিরাপদে জেনারেট করতে পারেন):

```bash
openssl rand -hex 32
```

SSL সেটআপের জন্য Caddy বা Nginx রিভার্স প্রোক্সি ব্যবহার করুন (Let's Encrypt)।

---

## সিকিউরিটি বেস্ট প্র্যাকটিস

- SSH নিরাপদ করুন (`PasswordAuthentication no`)

```bash
sudo nano /etc/ssh/sshd_config
```

- ফায়ারওয়াল সেট করুন:

```bash
sudo ufw allow 22
sudo ufw allow 80
sudo ufw allow 443
sudo ufw enable
```

- Fail2Ban ইনস্টল করুন:

```bash
sudo apt install fail2ban
```

- Redis ও Postgres-এ শক্তিশালী পাসওয়ার্ড ব্যবহার করুন।

---

## টেস্টিং ও ভেরিফিকেশন

- কন্টেইনার স্ট্যাটাস:

```bash
docker ps
```

- লগ চেক:

```bash
docker logs outline
```

- ব্রাউজারে ওপেন করুন: `http://your-server-ip:3000`

---

## অ্যাডভান্স টপিকস

- Performance: Redis ক্যাশিং ব্যবহার করুন।
- IPv6: Docker নেটওয়ার্কে IPv6 কনফিগার করুন।
- High availability: লোড ব্যালান্সার ও replicas ব্যবহার করুন।

---

## ট্রাবলশুটিং

**কন্টেইনার স্টার্ট হয় না:** লগ দেখুন

```bash
docker-compose logs
```

**ডাটাবেস কানেকশন এরর:** `.env`-এর `DATABASE_URL` চেক করুন।

---

## মেইনটেন্যান্স

- আপডেট:

```bash
docker-compose pull && docker-compose up -d
```

- কী রোটেশন:

```bash
openssl rand -hex 32
```

- নতুন ইউজার অ্যাড: Outline UI থেকে করুন।

---

## UI / ম্যানেজমেন্ট টুলস (ঐচ্ছিক)

- Portainer দিয়ে কন্টেইনার ম্যানেজ করুন।
- Outline Admin Dashboard থেকে ইউজার ও ডক ম্যানেজ করুন।

---

## ব্যাকআপ ও রিস্টোর

ব্যাকআপ:

```bash title="backup.sql"
docker exec -t postgres pg_dumpall -c -U outline > backup.sql
```

রিস্টোর:

```bash title="backup.sql"
docker exec -i postgres psql -U outline < backup.sql
```

---

## এক্সট্রা এনহ্যান্সমেন্ট

- মনিটরিং: Prometheus + Grafana
- Auto-restart: `systemctl enable docker`
- Alerting: Docker healthchecks কনফিগার করুন

---

✅ এতেই শেষ — আপনার টিমের জন্য Outline সেটআপ মূলত প্রস্তুত। 🎉

প্রয়োজন হলে SSL, রিভার্স প্রোক্সি, বা HA সেটআপে আরও বিস্তারিত দেব।

---

### অফিসিয়াল ডক

[https://github.com/outline/outline](https://github.com/outline/outline)