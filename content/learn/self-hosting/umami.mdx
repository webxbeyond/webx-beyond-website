---
title: "উমামি (Umami)"
icon: "mdi:server"
description: "Umami নিজে হোস্ট করে আপনার ওয়েবসাইট বা অ্যাপ্লিকেশনের লাইটওয়েট এবং প্রাইভেসি-ফ্রেন্ডলি অ্যানালাইটিক্স পেতে পারেন।"
keywords:
 - Umami
 - উমামি
 - website analytics
 - self-hosting umami
 - privacy-friendly analytics
---

# Umami (উমামি) সেলফ-হোস্টিং গাইড

## ভূমিকা
**Umami** হলো একটি হালকা, ওপেন-সোর্স এবং প্রাইভেসি-ফ্রেন্ডলি ওয়েব অ্যানালাইটিক্স প্ল্যাটফর্ম।  
এটি Google Analytics এর মতো কাজ করে, কিন্তু কোনো ট্র্যাকার ব্যবহার করে না এবং ডেটা সম্পূর্ণ আপনার সার্ভারে থাকে।  

### কেন নিজে হোস্ট করবেন?
- ডেটা প্রাইভেসি সম্পূর্ণ আপনার হাতে।
- হালকা এবং দ্রুত, কম রিসোর্স ব্যবহার।
- যেকোনো ওয়েবসাইট বা অ্যাপ্লিকেশনের ট্রাফিক ট্র্যাক করতে সক্ষম।
- কোনো তৃতীয় পক্ষের লিমিটেশন নেই।

### প্রধান ব্যবহার ক্ষেত্র
- ওয়েবসাইট ট্রাফিক মনিটরিং।
- পেজভিউ, ইউজার এবং সেশন বিশ্লেষণ।
- মাল্টি-প্রোপার্টি বা মাল্টি-ডোমেইন ট্র্যাকিং।
- API বা ড্যাশবোর্ড থেকে রিপোর্ট দেখা।

---

## ইনস্টলেশন পদ্ধতি

Umami **Docker Compose** ব্যবহার করে দ্রুত চালু করা যায়।

### ১. প্রয়োজনীয় সফটওয়্যার
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install docker.io docker-compose -y
```

---

### ২. প্রোজেক্ট ফোল্ডার তৈরি

```bash
mkdir umami && cd umami
```

---

### ৩. `.env` ফাইল তৈরি

```bash title=".env"
UMAMI_DB_URL=mysql://umami:yourpassword@db:3306/umami
UMAMI_PORT=3000
```

---

### ৪. Docker Compose কনফিগারেশন

```yaml title="docker-compose.yml"
services:
  db:
    image: mysql:8.0
    container_name: umami-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: umami
      MYSQL_USER: umami
      MYSQL_PASSWORD: yourpassword
    volumes:
      - ./db-data:/var/lib/mysql

  umami:
    image: ghcr.io/umami-software/umami:latest
    container_name: umami
    depends_on:
      - db
    ports:
      - "${UMAMI_PORT}:3000"
    environment:
      DATABASE_URL: ${UMAMI_DB_URL}
    restart: unless-stopped
```

---

### ৫. কন্টেইনার চালু করা

```bash
docker-compose up -d
```

> ✅ এখন Umami ওয়েব ইন্টারফেস চালু হয়েছে: `http://localhost:3000` বা `http://your-server-ip:3000`

প্রথমবার লগইনের জন্য Umami ড্যাশবোর্ডে অ্যাডমিন ইউজার তৈরি করতে হবে।

---

## সাধারণ সমস্যা ও সমাধান

* **পোর্ট 3000 ব্যস্ত:** `.env` ফাইলে `UMAMI_PORT` পরিবর্তন করুন।
* **ডেটাবেস সংযোগ সমস্যা:** `.env` ফাইলে `UMAMI_DB_URL` সঠিকভাবে সেট আছে কিনা চেক করুন।
* **লগ চেক করতে:**

```bash
docker logs -f umami
```

---

## টিপস

* Nginx Proxy Manager বা Traefik ব্যবহার করে SSL + কাস্টম ডোমেইন কনফিগার করুন।
* ডেটা লস এড়াতে `./db-data` ভলিউম ব্যাকআপ রাখুন।
* ওয়েবসাইটে ট্র্যাকিং কোড ইনস্টল করার সময় **প্রাইভেসি নীতি অনুযায়ী** ব্যবহার করুন।

---

## উপসংহার

**Umami** হলো হালকা, দ্রুত এবং প্রাইভেসি-ফোকাসড ওয়েব অ্যানালাইটিক্স টুল।
ডকারাইজড সেটআপ ব্যবহার করে এটি সহজে হোস্ট করা যায় এবং পুরো ডেটা আপনার নিয়ন্ত্রণে থাকে।

এটি ওয়েবসাইট বা অ্যাপ্লিকেশনের ট্রাফিক মনিটর করার জন্য একটি আদর্শ সমাধান। 🚀