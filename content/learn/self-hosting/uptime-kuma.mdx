---
title: "আপটাইম কুমা (Uptime Kuma)"
icon: "mdi:server"
description: "Uptime Kuma নিজে হোস্ট করে আপনার সার্ভার, ওয়েবসাইট এবং অ্যাপ্লিকেশনগুলোর আপটাইম মনিটরিং সহজে শুরু করুন।"
keywords:
 - Uptime Kuma
 - আপটাইম মনিটরিং
 - self-hosting uptime kuma
 - ডকার আপটাইম কুমা
 - server monitoring
---

## ভূমিকা
**Uptime Kuma** হলো একটি ওপেন-সোর্স আপটাইম মনিটরিং টুল যা দিয়ে আপনি আপনার সার্ভার, ওয়েবসাইট বা অ্যাপ্লিকেশন কতক্ষণ সচল আছে তা মনিটর করতে পারবেন।  
এটি একপ্রকার **"self-hosted alternative to Uptime Robot"**।  

### কেন নিজে হোস্ট করবেন?
- নিজের ডেটার উপর সম্পূর্ণ নিয়ন্ত্রণ।
- কোনো থার্ড-পার্টি সীমাবদ্ধতা ছাড়াই অসীম সংখ্যক সার্ভিস মনিটর করার সুবিধা।
- ফ্রি এবং কাস্টমাইজযোগ্য ড্যাশবোর্ড।
- নোটিফিকেশন সাপোর্ট (Discord, Telegram, Email, Slack ইত্যাদি)।

### প্রধান ব্যবহার ক্ষেত্রসমূহ
- ওয়েবসাইট আপটাইম মনিটরিং।
- সার্ভারের স্বাস্থ্য ও পিং মনিটর।
- API এন্ডপয়েন্ট ট্র্যাকিং।
- ডকার কন্টেইনার বা লোকাল সার্ভিস স্ট্যাটাস চেক।

---

## ইনস্টলেশন পদ্ধতি

Uptime Kuma খুব সহজেই **Docker** ব্যবহার করে সেটআপ করা যায়। নিচে ধাপে ধাপে দেখানো হলো:

### ১. প্রয়োজনীয় সফটওয়্যার
- **Docker** এবং **Docker Compose** আপনার সার্ভারে ইন্সটল থাকতে হবে।  
Ubuntu/Debian এ ইনস্টল করতে:
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install docker.io docker-compose -y
```

---

### ২. প্রোজেক্ট ফোল্ডার তৈরি

```bash
mkdir uptime-kuma && cd uptime-kuma
```

---

### ৩. `.env` ফাইল তৈরি (সেনসিটিভ ডেটার জন্য)

```bash title=".env"
UPTIME_KUMA_PORT=3001
UPTIME_KUMA_DATA=./data
```

---

### ৪. Docker Compose কনফিগারেশন

```yaml title="docker-compose.yml"
services:
  uptime-kuma:
    image: louislam/uptime-kuma:latest
    container_name: uptime-kuma
    restart: always
    ports:
      - "${UPTIME_KUMA_PORT}:3001"
    volumes:
      - ${UPTIME_KUMA_DATA}:/app/data
```

---

### ৫. কন্টেইনার চালু করা

```bash
docker-compose up -d
```

> ✅ এখন আপনার Uptime Kuma চলবে `http://localhost:3001` এ।
> রিমোট সার্ভারে হলে `http://your-server-ip:3001` দিয়ে অ্যাক্সেস করতে পারবেন।

---

## সাধারণ সমস্যা ও সমাধান

* **পোর্ট ব্যস্ত থাকলে:** `.env` ফাইলে `UPTIME_KUMA_PORT` পরিবর্তন করুন।
* **কনফিগারেশন আপডেট করতে:**

  ```bash
  docker-compose down
  docker-compose pull
  docker-compose up -d
  ```
* **লগ চেক করতে:**

  ```bash
  docker logs -f uptime-kuma
  ```

---

## টিপস

* Nginx Proxy Manager বা Traefik ব্যবহার করে **ডোমেইন + SSL** কনফিগার করুন।
* নোটিফিকেশন চ্যানেল সেট করতে Uptime Kuma এর **Settings > Notification** মেনু ব্যবহার করুন।
* ডেটা লস এড়াতে `./data` ভলিউম **ব্যাকআপ** রাখুন।

---

## উপসংহার

**Uptime Kuma** একটি শক্তিশালী ও ব্যবহারবান্ধব সেলফ-হোস্টেড টুল যা আপনাকে আপনার সার্ভিসগুলোর আপটাইম মনিটর করতে সাহায্য করবে।
ডকারাইজড সেটআপের মাধ্যমে খুব দ্রুত ও সহজে এটি চালু করা যায়।

এখন আপনি নিজের সার্ভার বা ওয়েবসাইট সবসময় সচল আছে কিনা, তা নিশ্চিত করতে পারবেন **Uptime Kuma** দিয়ে। 🚀