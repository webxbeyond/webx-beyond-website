---
title: "এন৮এন (n8n)"
icon: "mdi:server"
description: "n8n নিজে হোস্ট করে স্বয়ংক্রিয় ওয়ার্কফ্লো তৈরি এবং API ইন্টিগ্রেশন সহজে করুন।"
keywords:
 - n8n
 - এন৮এন
 - workflow automation
 - self-hosting n8n
 - API integration
---

# n8n (এন৮এন) সেলফ-হোস্টিং গাইড

## ভূমিকা
**n8n** হলো একটি ওপেন-সোর্স নো-কোড/লো-কোড **ওয়ার্কফ্লো অটোমেশন প্ল্যাটফর্ম**।  
এটির মাধ্যমে আপনি বিভিন্ন অ্যাপ্লিকেশন ও সার্ভিসের মধ্যে ডেটা স্থানান্তর, প্রসেস অটোমেশন এবং API ইন্টিগ্রেশন করতে পারেন।  

### কেন নিজে হোস্ট করবেন?
- সম্পূর্ণ ডেটা কন্ট্রোল এবং প্রাইভেসি।
- কোনো থার্ড-পার্টি সিমিতি নেই।
- কাস্টমাইজেবল এবং একাধিক ইনটিগ্রেশন।
- রিয়েল-টাইম ওয়ার্কফ্লো ট্র্যাকিং।
- স্বয়ংক্রিয় ইমেল, Slack, Discord, ও অন্যান্য API একত্রিতকরণ।

### প্রধান ব্যবহার ক্ষেত্রসমূহ
- বিভিন্ন SaaS অ্যাপ্লিকেশন ইন্টিগ্রেশন।
- ইমেল বা চ্যাট নোটিফিকেশন অটোমেশন।
- ডেটা প্রসেসিং এবং API কল অটোমেশন।
- Cron jobs ও শিডিউলড টাস্ক অটোমেশন।

---

## ইনস্টলেশন পদ্ধতি

n8n ডকারাইজড ইন্সটলেশন সবচেয়ে সহজ এবং প্রোডাকশন-রেডি।

### ১. প্রয়োজনীয় সফটওয়্যার
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install docker.io docker-compose -y
```

---

### ২. প্রোজেক্ট ফোল্ডার তৈরি

```bash
mkdir n8n && cd n8n
```

---

### ৩. `.env` ফাইল তৈরি

```bash title=".env"
# n8n Basic Authentication
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=username
N8N_BASIC_AUTH_PASSWORD=secure-password
GENERIC_TIMEZONE=Asia/Dhaka
N8N_MFA_ENABLED=true
N8N_ENCRYPTION_KEY=encryption-key

# Database Configuration
DB_TYPE=postgresdb
DB_HOST=db
DB_PORT=5432
DB_NAME=automation
DB_USER=username
DB_PASSWORD=secure-password

# Node Function External Access
NODE_FUNCTION_ALLOW_EXTERNAL=false

WEBHOOK_URL=webhook-url
N8N_SECURE_COOKIE=true
```

---

### ৪. Docker Compose কনফিগারেশন

```yaml title="docker-compose.yml"
services:
  n8n:
    image: n8nio/n8n
    container_name: n8n
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=${N8N_BASIC_AUTH_ACTIVE}
      - N8N_BASIC_AUTH_USER=${N8N_BASIC_AUTH_USER}
      - N8N_BASIC_AUTH_PASSWORD=${N8N_BASIC_AUTH_PASSWORD}
      - GENERIC_TIMEZONE=${GENERIC_TIMEZONE}
      - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY}
      - N8N_MFA_ENABLED=${N8N_MFA_ENABLED}
      - WEBHOOK_URL=${WEBHOOK_URL}
      - DB_TYPE=${DB_TYPE}
      - DB_POSTGRESDB_HOST=${DB_HOST}
      - DB_POSTGRESDB_PORT=${DB_PORT}
      - DB_POSTGRESDB_DATABASE=${DB_NAME}
      - DB_POSTGRESDB_USER=${DB_USER}
      - DB_POSTGRESDB_PASSWORD=${DB_PASSWORD}
      - NODE_FUNCTION_ALLOW_EXTERNAL=${NODE_FUNCTION_ALLOW_EXTERNAL}
    depends_on:
      - db
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    container_name: n8n-database
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    restart: unless-stopped
```

---

### ৫. কন্টেইনার চালু করা

```bash
docker-compose up -d
```

> ✅ n8n ওয়েব ইন্টারফেস: `http://localhost:5678` বা `http://your-server-ip:5678`

লগইন করতে `.env` ফাইলে থাকা ইউজারনেম এবং পাসওয়ার্ড ব্যবহার করুন।

---

## সাধারণ সমস্যা ও সমাধান

* **পোর্ট 5678 ব্যস্ত:** `.env` ফাইলে `N8N_PORT` পরিবর্তন করুন।
* **ডেটা লস এড়াতে:** `./n8n-data` ভলিউম ব্যাকআপ রাখুন।
* **লগ চেক করতে:**

```bash
docker logs -f n8n
```

* **HTTPS ব্যবহার করতে:** Nginx Proxy Manager বা Traefik এর মাধ্যমে SSL টার্মিনেশন করুন।

---

## টিপস

* ক্রন ভিত্তিক ওয়ার্কফ্লো চালানোর জন্য n8n এর “Trigger” নোড ব্যবহার করুন।
* বিভিন্ন SaaS অ্যাপ্লিকেশনের জন্য প্রি-বিল্ট কনেক্টর ব্যবহার করুন।
* প্রোডাকশনে চালানোর জন্য **Persistent Volume + SSL + Basic Auth** ব্যবহার করা আবশ্যক।

---

## উপসংহার

**n8n** হলো সহজ, শক্তিশালী এবং প্রাইভেসি-ফোকাসড ওয়ার্কফ্লো অটোমেশন টুল।
ডকারাইজড সেটআপ ব্যবহার করে এটি দ্রুত এবং নিরাপদভাবে হোস্ট করা যায়।

এটির সাহায্যে আপনি নিজস্ব API এবং সার্ভিসের সাথে পূর্ণ নিয়ন্ত্রণ ও অটোমেশন তৈরি করতে পারবেন। 🚀