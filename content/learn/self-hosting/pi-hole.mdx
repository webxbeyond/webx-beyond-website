---
title: "পাই-হোল (Pi-hole)"
icon: "mdi:server"
description: "Pi-hole নিজে হোস্ট করে নেটওয়ার্ক-লেভেলে বিজ্ঞাপন ও ট্র্যাকার ব্লক করার সম্পূর্ণ নির্দেশিকা।"
keywords:
 - Pi-hole
 - পাই-হোল
 - self-hosting pi-hole
 - network ad blocker
 - DNS filtering
---

## ভূমিকা
**Pi-hole** হলো একটি নেটওয়ার্ক-লেভেলের বিজ্ঞাপন ব্লকার (network-wide ad blocker)।  
এটি মূলত একটি DNS sinkhole যা অপ্রয়োজনীয় বিজ্ঞাপন, ট্র্যাকার ও ম্যালিশিয়াস ডোমেইন ব্লক করে দেয়।  

### কেন Pi-hole নিজে হোস্ট করবেন?
- আপনার **পুরো নেটওয়ার্কে বিজ্ঞাপন ব্লক** হবে, শুধু ব্রাউজারে নয়।
- ফ্রি এবং ওপেন-সোর্স সল্যুশন।
- কম রিসোর্স ব্যবহার করে (Raspberry Pi, VPS, বা যেকোনো Linux সার্ভারে চালানো যায়)।
- গোপনীয়তা সুরক্ষা (Privacy Protection)।
- বিস্তারিত **স্ট্যাটিস্টিক্স ড্যাশবোর্ড** পাওয়া যায়।

### প্রধান ব্যবহার ক্ষেত্র
- ঘরের WiFi নেটওয়ার্কে বিজ্ঞাপন ব্লক করা।
- কর্পোরেট বা টিম নেটওয়ার্কে অবাঞ্ছিত সাইট/ট্র্যাকার ফিল্টার করা।
- লাইটওয়েট DNS সার্ভার হিসেবে ব্যবহার।

---

## ইনস্টলেশন পদ্ধতি

Pi-hole **Docker** ব্যবহার করে খুব সহজে ইনস্টল করা যায়।

### ১. প্রয়োজনীয় সফটওয়্যার
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install docker.io docker-compose -y
```

---

### ২. প্রোজেক্ট ফোল্ডার তৈরি

```bash
mkdir pi-hole && cd pi-hole
```

---

### ৩. `.env` ফাইল তৈরি

```bash title=".env"
PIHOLE_PORT=8080
DNS_PORT=53
PIHOLE_TIMEZONE=Asia/Dhaka
PIHOLE_PASSWORD=your-secure-password
```

---

### ৪. Docker Compose কনফিগারেশন

```yaml title="docker-compose.yml"
services:
  pihole:
    container_name: pihole
    image: pihole/pihole:latest
    restart: unless-stopped
    ports:
      - "${DNS_PORT}:53/tcp"
      - "${DNS_PORT}:53/udp"
      - "${PIHOLE_PORT}:80"
    environment:
      TZ: "${PIHOLE_TIMEZONE}"
      WEBPASSWORD: "${PIHOLE_PASSWORD}"
    volumes:
      - ./etc-pihole/:/etc/pihole/
      - ./etc-dnsmasq.d/:/etc/dnsmasq.d/
    cap_add:
      - NET_ADMIN
```

---

### ৫. কন্টেইনার চালু করা

```bash
docker-compose up -d
```

---

### ৬. ওয়েব ইন্টারফেসে লগইন

Pi-hole ওয়েব UI অ্যাক্সেস করুন:
`http://your-server-ip:8080/admin`

লগইন করার জন্য `.env` ফাইলে দেওয়া **PIHOLE\_PASSWORD** ব্যবহার করুন।

---

## সাধারণ সমস্যা ও সমাধান

* **DNS Port 53 ব্যস্ত থাকলে:** অন্য কোনো সার্ভিস (যেমন systemd-resolved) এটি ব্যবহার করছে। সমাধান:

  ```bash
  sudo systemctl stop systemd-resolved
  sudo systemctl disable systemd-resolved
  ```
* **অ্যাড ব্লক কাজ না করলে:** নিশ্চিত করুন আপনার ডিভাইসগুলোতে Pi-hole এর IP **Primary DNS Server** হিসেবে দেওয়া আছে।
* **লগ দেখার জন্য:**

  ```bash
  docker logs -f pihole
  ```

---

## টিপস

* ডোমেইন-ভিত্তিক ব্লকলিস্ট আপডেট করতে Pi-hole ড্যাশবোর্ড ব্যবহার করুন।
* **Unbound DNS resolver** এর সাথে ইন্টিগ্রেট করলে Pi-hole আরো শক্তিশালী হবে।
* Pi-hole এর জন্য একটি **Static IP** ব্যবহার করা উত্তম।

---

## উপসংহার

**Pi-hole** একটি শক্তিশালী ও হালকা টুল যা আপনার নেটওয়ার্কে অপ্রয়োজনীয় বিজ্ঞাপন ও ট্র্যাকার ব্লক করে দেয়।
নিজে হোস্ট করলে আপনি পুরো নেটওয়ার্কের উপর নিয়ন্ত্রণ পাবেন এবং গোপনীয়তাও বজায় থাকবে।

ডকার কন্টেইনার ব্যবহার করে এটি কয়েক মিনিটেই চালু করা যায় এবং Raspberry Pi এর মতো সাশ্রয়ী হার্ডওয়্যারেও দারুণ কাজ করে। 🚀