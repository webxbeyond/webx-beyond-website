---
title: "অ্যাডগার্ড হোম (AdGuard Home)"
icon: "mdi:server"
description: "AdGuard Home নিজে হোস্ট করে নেটওয়ার্ক-লেভেলে বিজ্ঞাপন, ট্র্যাকার এবং মালওয়্যার ব্লক করা সহজ।"
keywords:
 - AdGuard Home
 - অ্যাডগার্ড হোম
 - self-hosting adguard home
 - network ad blocker
 - DNS filtering
---

## ভূমিকা
**AdGuard Home** হলো একটি নেটওয়ার্ক-লেভেল অ্যাড ব্লকার এবং DNS সার্ভার।  
এটি Pi-hole এর মতো কাজ করে কিন্তু আরও কিছু অতিরিক্ত ফিচার সহ আসে যেমন পারফরম্যান্স অপ্টিমাইজেশন, ফিল্টার সেটিংস এবং উন্নত ইউজার ইন্টারফেস।  

### কেন নিজে হোস্ট করবেন?
- পুরো নেটওয়ার্কে বিজ্ঞাপন ও ট্র্যাকার ব্লক।
- ফ্রি, ওপেন-সোর্স এবং সম্পূর্ণ কাস্টমাইজযোগ্য।
- পারিবারিক ফিল্টারিং এবং নিরাপদ ব্রাউজিং সুবিধা।
- রিয়েল-টাইম স্ট্যাটিস্টিক্স এবং রিপোর্ট।
- VPN বা রিমোট নেটওয়ার্কেও কার্যকর।

### প্রধান ব্যবহার ক্ষেত্র
- হোম WiFi নেটওয়ার্কে বিজ্ঞাপন ও ট্র্যাকার ব্লক।
- ছোট অফিস বা টিম নেটওয়ার্কে DNS ফিল্টারিং।
- পারিবারিক/কিডস ব্রাউজিং ফিল্টার তৈরি।
- নেটওয়ার্ক মনিটরিং ও লগিং।

---

## ইনস্টলেশন পদ্ধতি

AdGuard Home খুব সহজে **Docker** ব্যবহার করে ইন্সটল করা যায়।

### ১. প্রয়োজনীয় সফটওয়্যার
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install docker.io docker-compose -y
```

---

### ২. প্রোজেক্ট ফোল্ডার তৈরি

```bash
mkdir adguard-home && cd adguard-home
```

---

### ৩. `.env` ফাইল তৈরি

```bash title=".env"
ADGUARD_PORT=3000
DNS_PORT=53
ADGUARD_DATA=./data
```

---

### ৪. Docker Compose কনফিগারেশন

```yaml title="docker-compose.yml"
services:
  adguardhome:
    container_name: adguardhome
    image: adguard/adguardhome:latest
    restart: unless-stopped
    ports:
      - "${ADGUARD_PORT}:3000/tcp"
      - "${DNS_PORT}:53/tcp"
      - "${DNS_PORT}:53/udp"
    volumes:
      - ${ADGUARD_DATA}:/opt/adguardhome/work
      - ${ADGUARD_DATA}:/opt/adguardhome/conf
```

---

### ৫. কন্টেইনার চালু করা

```bash
docker-compose up -d
```

> ✅ এখন AdGuard Home চলবে: `http://your-server-ip:3000`

প্রথমবার লগইনের সময় ওয়েব ইন্টারফেসে সেটআপ উইজার্ড সম্পূর্ণ করুন।

---

## সাধারণ সমস্যা ও সমাধান

* **DNS Port 53 ব্যস্ত থাকলে:**
  অন্য সার্ভিস যেমন `systemd-resolved` 53 পোর্ট ব্যবহার করতে পারে।
  সমাধান:

  ```bash
  sudo systemctl stop systemd-resolved
  sudo systemctl disable systemd-resolved
  ```
* **লগ চেক করতে:**

  ```bash
  docker logs -f adguardhome
  ```
* **ফিল্টার কাজ না করলে:**
  নিশ্চিত করুন ক্লায়েন্ট ডিভাইসগুলো DNS হিসেবে AdGuard Home সার্ভারের IP ব্যবহার করছে।

---

## টিপস

* ডোমেইন-ভিত্তিক ব্লকলিস্ট ও হোয়াইটলিস্ট ব্যবহার করে কাস্টমাইজড ফিল্টার তৈরি করুন।
* Nginx Proxy Manager বা Traefik দিয়ে SSL সহ কাস্টম ডোমেইন কনফিগার করুন।
* ডেটা লস এড়াতে `./data` ভলিউমের ব্যাকআপ রাখুন।
* পারিবারিক ফিল্টার ব্যবহার করতে **Clients Settings** থেকে নির্দিষ্ট IP ব্লক/Allow করুন।

---

## উপসংহার

**AdGuard Home** হলো হালকা, শক্তিশালী এবং ব্যবহারবান্ধব নেটওয়ার্ক-লেভেল অ্যাড ব্লকার।
ডকারাইজড সেটআপ ব্যবহার করে এটি খুব সহজে চালু করা যায় এবং পুরো নেটওয়ার্কের উপর নিয়ন্ত্রণ পাওয়া যায়।

এটি আপনার নেটওয়ার্ককে নিরাপদ ও বিজ্ঞাপন-মুক্ত রাখার জন্য আদর্শ সল্যুশন। 🚀