---
title: "ফ্রেশআরএসএস (FreshRSS)"
icon: "mdi:server"
description: "FreshRSS সেলফ হোস্টিং করার পূর্ণাঙ্গ গাইড। সহজ ধাপে ধাপে নির্দেশনা সহ ইনস্টলেশন, কনফিগারেশন এবং Docker Compose উদাহরণ।"
keywords: 
 - FreshRSS
 - RSS ফিড রিডার
 - সেলফ হোস্টিং
 - Docker
 - নিউজ রিডার
---

## ১. ভূমিকা (Introduction)

**FreshRSS** একটি লাইটওয়েট ও শক্তিশালী ওপেন-সোর্স **RSS ফিড অ্যাগ্রেগেটর**।  
এটি ব্যবহার করে আপনি আপনার পছন্দের ব্লগ, নিউজ সাইট, ইউটিউব চ্যানেল বা অন্য যেকোনো RSS ফিড এক জায়গায় সাবস্ক্রাইব ও পড়তে পারবেন।

### কেন FreshRSS নিজে হোস্ট করবেন?
- ✅ **নিজস্ব কন্ট্রোল** – আপনার ডেটা নিজের সার্ভারে নিরাপদ থাকবে।  
- ✅ **অ্যাড-ফ্রি অভিজ্ঞতা** – কোনো বিজ্ঞাপন বা ট্র্যাকিং থাকবে না।  
- ✅ **একাধিক ইউজার সাপোর্ট** – পরিবার বা টিম মেম্বারদের জন্য ব্যবহারযোগ্য।  
- ✅ **API সাপোর্ট** – মোবাইল অ্যাপ (যেমন FeedMe, Fluent Reader) এর সাথে কানেক্ট করা যাবে।  

### প্রধান ব্যবহার ক্ষেত্র
- ব্যক্তিগত নিউজ রিডার  
- টিম বা প্রতিষ্ঠানের জন্য শেয়ার্ড ফিড ম্যানেজমেন্ট  
- অফলাইনে আর্টিকেল রিডিং সাপোর্ট  

---

## ২. ইনস্টলেশন পদ্ধতি (Installation Methods)

FreshRSS সহজেই **Docker** ব্যবহার করে চালানো যায়। নিচে ধাপে ধাপে সেটআপ দেখানো হলো।

### ধাপ ১: সার্ভার প্রস্তুতি
আপনার সার্ভারে আগে থেকেই **Docker** এবং **Docker Compose** ইনস্টল করা থাকতে হবে।  

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install docker docker-compose -y
```

### ধাপ ২: প্রোজেক্ট ফোল্ডার তৈরি

```bash
mkdir freshrss && cd freshrss
```

### ধাপ ৩: `.env` ফাইল তৈরি

সেন্সিটিভ ডেটা স্টোর করার জন্য `.env` ফাইল ব্যবহার করুন।

```bash title=".env"
MYSQL_USER=freshrss
MYSQL_PASSWORD=securepassword
MYSQL_DATABASE=freshrss
MYSQL_ROOT_PASSWORD=rootpassword
```

### ধাপ ৪: Docker Compose ফাইল তৈরি

```yaml title="docker-compose.yml"
services:
  freshrss:
    image: freshrss/freshrss:latest
    container_name: freshrss
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./data:/var/www/FreshRSS/data
    environment:
      - CRON_MIN=5
    depends_on:
      - db

  db:
    image: mysql:8
    container_name: freshrss-db
    restart: unless-stopped
    environment:
      MYSQL_USER=${MYSQL_USER}
      MYSQL_PASSWORD=${MYSQL_PASSWORD}
      MYSQL_DATABASE=${MYSQL_DATABASE}
      MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
    volumes:
      - ./db_data:/var/lib/mysql

volumes:
  db_data:
```

### ধাপ ৫: কন্টেইনার চালু করুন

```bash
docker-compose up -d
```

### ধাপ ৬: ওয়েব ইন্টারফেসে প্রবেশ

ব্রাউজারে গিয়ে ওপেন করুন:
👉 `http://your-server-ip:8080`

প্রথমবার সেটআপে আপনাকে:

1. **ডাটাবেস কানেকশন ডিটেইলস** দিতে হবে (যা `.env` ফাইলে আছে)।
2. **অ্যাডমিন ইউজার** তৈরি করতে হবে।

---

## ৩. দরকারি টিপস ও কনফিগারেশন

* 🔄 **অটো ফিড আপডেট**: Docker Compose এ `CRON_MIN=5` মানে প্রতি ৫ মিনিটে ফিড আপডেট হবে।
* 🔐 **সিকিউরিটি**: প্রোডাকশনে ব্যবহার করার সময় Nginx Proxy Manager বা Caddy দিয়ে **HTTPS (SSL)** সেটআপ করুন।
* 👥 **মাল্টি-ইউজার সাপোর্ট**: একাধিক ইউজার যোগ করা যায় → `Administration > Users` থেকে।
* 📱 **মোবাইল অ্যাপ সাপোর্ট**: FreshRSS এর API দিয়ে **FeedMe** বা **Fluent Reader** এর মতো অ্যাপের সাথে কানেক্ট করতে পারবেন।

---

## ৪. সমস্যা সমাধান (Troubleshooting)

* **কন্টেইনার লগ দেখতে:**

```bash
docker logs freshrss -f
```

* **ডাটাবেস সমস্যা হলে:**

```bash
docker-compose down
docker volume rm freshrss_db_data
docker-compose up -d
```

* **পোর্ট পরিবর্তন করতে চাইলে:**
  `docker-compose.yml` এ `8080:80` পরিবর্তন করুন।

---

## ৫. উপসংহার

FreshRSS একটি হালকা, শক্তিশালী এবং ফিচার-সমৃদ্ধ **সেলফ হোস্টেড RSS রিডার**।
এই গাইড অনুসরণ করলে সহজেই নিজের সার্ভারে FreshRSS চালু করতে পারবেন।