---
title: "নেটডাটা (Netdata)"
icon: "mdi:server"
description: "Netdata সেলফ হোস্টিং করার জন্য পূর্ণাঙ্গ বাংলা গাইড। সার্ভার মনিটরিং, Docker Compose উদাহরণ, এবং ধাপে ধাপে ইনস্টলেশন নির্দেশনা।"
keywords: 
 - Netdata
 - সার্ভার মনিটরিং
 - সেলফ হোস্টিং
 - Docker
 - সিস্টেম পারফরম্যান্স
---

## ১. ভূমিকা (Introduction)

**Netdata** একটি রিয়েল-টাইম **সার্ভার ও সিস্টেম মনিটরিং টুল**।  
এটি ব্যবহার করে আপনি আপনার সার্ভারের CPU, RAM, Disk, Network, Database, Docker container সহ বিভিন্ন মেট্রিকস লাইভ দেখতে পারবেন।  

### কেন Netdata নিজে হোস্ট করবেন?
- ✅ **রিয়েল-টাইম মনিটরিং** – সিস্টেম লোড, প্রসেস, সার্ভিস, এবং কন্টেইনারগুলো তাত্ক্ষণিক দেখা যাবে।  
- ✅ **ভিজ্যুয়াল ড্যাশবোর্ড** – সুন্দর গ্রাফ ও চার্ট সহ ইন্টারেক্টিভ UI।  
- ✅ **হালকা ও দ্রুত** – খুব কম রিসোর্স ব্যবহার করে।  
- ✅ **অ্যালার্ট সিস্টেম** – সার্ভারের সমস্যার আগাম সতর্কবার্তা।  
- ✅ **ফ্রি ও ওপেন-সোর্স**  

### প্রধান ব্যবহার ক্ষেত্র
- ব্যক্তিগত VPS বা ডেডিকেটেড সার্ভার মনিটরিং  
- Docker container এর পারফরম্যান্স দেখা  
- ছোট থেকে বড় প্রতিষ্ঠানের সার্ভার ফ্লিট ট্র্যাক করা  

---

## ২. ইনস্টলেশন পদ্ধতি (Installation Methods)

Netdata সহজে **Docker** ব্যবহার করে চালানো যায়। নিচে ধাপে ধাপে সেটআপ দেওয়া হলো।

### ধাপ ১: সার্ভার প্রস্তুতি
আপনার সার্ভারে আগে থেকেই **Docker** এবং **Docker Compose** ইনস্টল থাকতে হবে।

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install docker docker-compose -y
```

### ধাপ ২: প্রোজেক্ট ফোল্ডার তৈরি

```bash
mkdir netdata && cd netdata
```

### ধাপ ৩: `.env` ফাইল তৈরি

```bash title=".env"
NETDATA_PORT=19999
```

### ধাপ ৪: Docker Compose ফাইল তৈরি

```yaml title="docker-compose.yml"
services:
  netdata:
    image: netdata/netdata:stable
    container_name: netdata
    hostname: netdata.local
    ports:
      - "${NETDATA_PORT}:19999"
    cap_add:
      - SYS_PTRACE
      - NET_ADMIN
    security_opt:
      - apparmor:unconfined
    volumes:
      - netdataconfig:/etc/netdata
      - netdatalib:/var/lib/netdata
      - netdatacache:/var/cache/netdata
      - /etc/passwd:/host/etc/passwd:ro
      - /etc/group:/host/etc/group:ro
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /etc/os-release:/host/etc/os-release:ro
    restart: unless-stopped

volumes:
  netdataconfig:
  netdatalib:
  netdatacache:
```

### ধাপ ৫: কন্টেইনার চালু করুন

```bash
docker-compose up -d
```

### ধাপ ৬: ওয়েব ইন্টারফেসে প্রবেশ

ব্রাউজারে গিয়ে ওপেন করুন:
👉 `http://your-server-ip:19999`

---

## ৩. দরকারি টিপস ও কনফিগারেশন

* 🔐 **সিকিউরিটি**: Netdata ডিফল্টভাবে পাসওয়ার্ড ছাড়া চালু হয়। প্রোডাকশন সার্ভারে অবশ্যই **Nginx Proxy Manager বা Traefik** দিয়ে **SSL + Basic Auth** যোগ করুন।
* 📊 **Docker কন্টেইনার মনিটরিং**: Netdata আপনার Docker container গুলোকেও স্বয়ংক্রিয়ভাবে ট্র্যাক করবে।
* 📡 **রিমোট অ্যাক্সেস কন্ট্রোল**: ফায়ারওয়াল দিয়ে `19999` পোর্ট সীমিত করে দিন অথবা VPN এর মাধ্যমে অ্যাক্সেস করুন।

---

## ৪. সমস্যা সমাধান (Troubleshooting)

* **লগ চেক করতে:**

```bash
docker logs netdata -f
```

* **পোর্ট পরিবর্তন করতে চাইলে:**
  `.env` ফাইলে `NETDATA_PORT` পরিবর্তন করুন এবং কন্টেইনার রিস্টার্ট করুন।

* **কন্টেইনার রিস্টার্ট:**

```bash
docker-compose restart netdata
```

---

## ৫. উপসংহার

**Netdata** একটি শক্তিশালী, হালকা ও রিয়েল-টাইম সার্ভার মনিটরিং টুল।
এই গাইড অনুসরণ করলে আপনি সহজেই নিজের সার্ভারে Netdata চালু করতে পারবেন।
