---
title: গিট ও গিটহাব পরিচিতি
icon: teenyicons:git-solid
---

## 🔰 গিট কী?
**Git** হলো একটি ডিসট্রিবিউটেড ভার্সন কন্ট্রোল সিস্টেম (DVCS) — যেখানে প্রতিটি ডেভেলপার তার লোকাল মেশিনে সম্পূর্ণ রেপোর ইতিহাস রাখে। এটি কোডের পরিবর্তন (history) ট্র্যাক, সহযোগিতা (collaboration) সহজ করা ও পরীক্ষামূলক ব্রাঞ্চে কাজের সুযোগ দেয়।

## 🆚 Git বনাম GitHub
| বিষয় | Git | GitHub |
|------|-----|--------|
| কী | টুল (CLI) | হোস্টিং প্ল্যাটফর্ম |
| কাজ | কোড ট্র্যাকিং | রিমোট রেপো + PR + Issues |
| অফলাইন | পূর্ণ ইতিহাসসহ কাজ করা যায় | প্রয়োজন নেই | 
| বিকল্প | Mercurial, Fossil | GitLab, Gitea, Bitbucket |

## 🧠 মূল ধারণা (Core Concepts)
- **Repository (Repo):** প্রজেক্ট + `.git` ফোল্ডার সহ ইতিহাস।
- **Commit:** ছোট লজিক্যাল স্ন্যাপশট (কেন পরিবর্তন, কী ছিল)।
- **Branch:** আলাদা কাজের লাইন (feature / fix / experiment)।
- **Merge / Rebase:** পরিবর্তন ইন্টিগ্রেট করার কৌশল।
- **Remote:** অনলাইন কপি (`origin`)।
- **HEAD:** আপনি বর্তমানে কোথায় (কোন ব্রাঞ্চ/কমিট) আছেন তার পয়েন্টার।

## ⚙️ গিট প্রাথমিক কনফিগ
```bash
git config --global user.name "আপনার নাম"
git config --global user.email "you@example.com"
git config --global init.defaultBranch main
git config --global core.autocrlf input   # লিনাক্স/ম্যাক
```

## 🚀 বেসিক ওয়ার্কফ্লো
```bash
git init                      # নতুন রেপো
git clone <url>               # রিমোট কপি নামানো
git status                    # অবস্থা দেখুন
git add file.js               # স্টেজ
git commit -m "feat: add api" # কমিট
git branch -M main            # ব্রাঞ্চ নাম পরিবর্তন
git remote add origin <url>   # রিমোট সেট
git push -u origin main       # প্রথম push
```

## 🛠 অপরিহার্য কমান্ড সারাংশ
| কাজ | কমান্ড |
|-----|--------|
| নতুন ব্রাঞ্চ | `git switch -c feature/ui` |
| ব্রাঞ্চে যাওয়া | `git switch feature/ui` |
| ব্রাঞ্চ তালিকা | `git branch --all` |
| স্টেজ সব | `git add .` |
| আনস্টেজ | `git restore --staged file.js` |
| শেষ কমিট সংশোধন | `git commit --amend` |
| হিস্ট্রি | `git log --oneline --graph --decorate --all` |
| ডিফ | `git diff` / `git diff --staged` |
| মার্জ | `git merge feature/ui` |
| রিবেইস | `git rebase main` |
| পুল (fetch+merge) | `git pull` |
| শুধু fetch | `git fetch --prune` |
| রিমোট ব্রাঞ্চ মুছুন | `git push origin --delete feature/ui` |

## 🧪 ব্রাঞ্চিং ফ্লো (উদাহরণ)
1. `main` = স্থিতিশীল
2. `feat/*` = নতুন ফিচার
3. `fix/*` = বাগ ফিক্স
4. `chore/*` = টুলিং / কনফিগ
5. Pull Request / Merge Request দিয়ে কোড রিভিউ

## 🧹 .gitignore বেসিক
`node_modules`, `.env`, build আউটপুট, লগ — রেপোতে না রাখা উত্তম।
```gitignore
node_modules/
.env
dist/
*.log
.DS_Store
```

## ❗ সাধারণ ভুল ও দ্রুত সমাধান
| সমস্যা | সমাধান |
|--------|--------|
| ভুল ব্রাঞ্চে কমিট | `git switch সঠিক-ব্রাঞ্চ` + `git cherry-pick <hash>` |
| স্টেজ না করে কমিট | `git add -p` (ইন্টারেকটিভ) |
| বড় ফাইল push | Git LFS বিবেচনা করুন |
| কনফ্লিক্ট | ফাইল ম্যানুয়ালি ঠিক করে `git add` → `git commit` |

## 🔄 রিবেইস বনাম মার্জ (সংক্ষেপ)
| দিক | Merge | Rebase |
|-----|-------|--------|
| ইতিহাস | ব্রাঞ্চিং স্পষ্ট | সরল লিনিয়ার |
| কনফ্লিক্ট | একবারে | প্রতি কমিটে হতে পারে |
| ব্যবহার | পাবলিক ব্রাঞ্চ | লোকাল / feature ব্রাঞ্চ |

## ✅ কমিট মেসেজ ফরম্যাট (সাজেশন)
```
<type>: <summary>

feat: add user login api
fix: handle null in queue worker
docs: update readme badge
```

## 📦 Stash (অস্থায়ী সেভ)
```bash
git stash push -m "wip: form validation"
git stash list
git stash pop
```

## 🧭 পরবর্তী কী?
- রিমোট সহযোগিতা (Fork, Pull Request)
- GitHub Actions বেসিক CI
- সাবমডিউল / মনোরেপো কৌশল

> প্রশ্ন / অনুশীলন আইডিয়া চাইলে ইস্যু ওপেন করুন।

শেখা চালিয়ে যান – ভার্সন কন্ট্রোল প্র্যাকটিস আপনার প্রোডাক্টিভিটি নাটকীয়ভাবে বাড়াবে। 🚀