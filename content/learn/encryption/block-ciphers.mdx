---
title: ব্লক সাইফার (Block Ciphers)
icon: mdi:lock
---

# ব্লক সাইফার: নিরাপত্তার ইট-সিমেন্ট


### ব্লক সাইফার (Block Cipher) কী?

**Block Cipher** হলো এক ধরনের **সিমেট্রিক এনক্রিপশন অ্যালগরিদম** যেখানে ডেটাকে ছোট ছোট নির্দিষ্ট আকারের ব্লকে (সাধারণত 64-bit বা 128-bit) ভাগ করে এনক্রিপ্ট করা হয়।

মানে, পুরো মেসেজকে একসাথে না করে **একেকটা নির্দিষ্ট সাইজের ব্লক নিয়ে আলাদা আলাদা করে এনক্রিপশন করা হয়**।

🔑 এখানে এনক্রিপ্ট ও ডিক্রিপ্ট করার জন্য একই কী (secret key) ব্যবহার করা হয়।

---

### কিভাবে কাজ করে?

1. মেসেজ → ব্লকে ভাগ করা হয় (যেমন: প্রতি ব্লক 128-bit)।
2. প্রতিটি ব্লকে একই কী ব্যবহার করে এনক্রিপশন চালানো হয়।
3. শেষে সব ব্লক একত্রে জোড়া লাগিয়ে এনক্রিপ্টেড ডেটা তৈরি হয়।
4. ডিক্রিপশনের সময় একই কী ব্যবহার করে উল্টোদিকে কাজ হয়।

---

### উদাহরণ (বাংলায়)

ধরুন আপনার মেসেজ হলো:

```
HELLO WORLD
```

👉 ধাপগুলো:

* প্রতিটি অক্ষরকে Binary (ASCII) তে রূপান্তর করা হয়।
* Block Cipher (যেমন AES) একে **১২৮-বিট ব্লকে ভাগ করে** এনক্রিপ্ট করবে।
* এনক্রিপ্ট হওয়ার পর পাওয়া যাবে একদম অচেনা ডেটা, যেমন:

```
10101100 11011010 00101101 ...
```

এগুলো ডিক্রিপ্ট করতে হলে **একই কী ব্যবহার করে উল্টোদিকে প্রক্রিয়া চালাতে হবে**।

---

### সহজ উপমা 🎁

ভাবুন, আপনার কাছে ১০০টা বই আছে।

* Block Cipher মানে হলো, আপনি প্রতিবারে নির্দিষ্ট আকারের বাক্সে (ধরুন ১০টা বই করে) রেখে তালা মেরে দিচ্ছেন।
* প্রতিটি বাক্স একই তালা (কী) দিয়ে লক করা হচ্ছে।
* যখন খুলতে চাইবেন, তখন একই চাবি দিয়ে একে একে প্রতিটি বাক্স খুলতে হবে।

---

## Mode of Operation: পরিচিতি

**Mode of Operation** হলো **block cipher** ব্যবহার করার নিয়ম।
Block cipher সাধারণত নির্দিষ্ট সাইজের ব্লকে (যেমন 64-bit বা 128-bit) ডেটা এনক্রিপ্ট করে। কিন্তু বাস্তবে আমাদের ডেটা অনেক বড় হতে পারে। তখন **মোড অফ অপারেশন** ঠিক করে দেয় কিভাবে:

1. একাধিক ব্লক এনক্রিপ্ট করবেন
2. কী এবং সিকোয়েন্স কিভাবে ব্যবহার হবে
3. পূর্বের ব্লকের আউটপুট নতুন ব্লকের এনক্রিপশনে কিভাবে প্রভাব ফেলবে

সাধারণভাবে, মোড অফ অপারেশন **ডেটার নিরাপত্তা, randomness এবং integrity** নির্ধারণ করে।

---

### প্রধান Block Cipher Modes of Operation

### 1. ECB (Electronic Codebook)

* প্রতিটি ব্লক আলাদা আলাদা এনক্রিপ্ট হয়
* সহজ, কিন্তু নিরাপত্তায় দুর্বল
* **সমস্যা:** একই plaintext ব্লক সবসময় একই ciphertext তৈরি করে → pattern লিক হতে পারে
* ব্যবহার: খুব সীমিত (ছোট ডেটা বা টেস্টিং)

---

### 2. CBC (Cipher Block Chaining)

* প্রতিটি plaintext ব্লক **পূর্বের ciphertext ব্লকের সাথে XOR** করা হয় তারপর এনক্রিপ্ট করা হয়
* প্রথম ব্লকের জন্য **IV (Initialization Vector)** ব্যবহার করা হয়
* ভালো security, pattern hide করে
* ব্যবহার: ফাইল এনক্রিপশন, SSL/TLS

---

### 3. CFB (Cipher Feedback)

* CBC-এর মতো, কিন্তু streaming-friendly
* ব্লক এনক্রিপশনকে **bit বা byte stream** এর মতো ব্যবহার করা যায়
* ব্যবহার: real-time communication, network stream

---

### 4. OFB (Output Feedback)

* Encryption output কে feedback হিসেবে ব্যবহার করে next block তৈরি হয়
* Decryption সহজ, stream cipher এর মতো কাজ করে
* ব্যবহার: error propagation কমাতে

---

### 5. CTR (Counter Mode)

* প্রতিটি ব্লক এনক্রিপশনের জন্য counter ব্যবহার হয়
* **parallelizable** → দ্রুত
* stream cipher এর মতো ব্যবহার করা যায়
* ব্যবহার: high-speed networking, disk encryption

---

### 6. GCM (Galois/Counter Mode)

* CTR + Authentication
* শুধু confidentiality নয়, **integrity & authenticity** নিশ্চিত করে
* ব্যবহৃত হয় HTTPS, VPN, cloud storage ইত্যাদিতে

---

### সারসংক্ষেপ

| Mode | Security      | Parallelizable | Use Case                         |
| ---- | ------------- | -------------- | -------------------------------- |
| ECB  | Weak          | Yes            | Small/test data                  |
| CBC  | Strong        | No             | File encryption                  |
| CFB  | Medium        | No             | Streaming data                   |
| OFB  | Medium        | No             | Streaming, low error propagation |
| CTR  | Strong        | Yes            | High-speed encryption            |
| GCM  | Strong + Auth | Yes            | HTTPS, cloud storage             |

---

**সারসংক্ষেপে:**
**Mode of Operation = block cipher কে বড় ডেটা বা streaming ডেটার সাথে নিরাপদভাবে ব্যবহার করার নিয়ম।**
প্রধানত 6 ধরনের মোড ব্যবহৃত হয়: ECB, CBC, CFB, OFB, CTR, এবং GCM।

## জনপ্রিয় ব্লক সাইফার

--- 

## 1. DES (Data Encryption Standard)

### ভূমিকা

ক্রিপ্টোগ্রাফির ইতিহাসে **DES (Data Encryption Standard)** একটি মাইলফলক। এটি ১৯৭০-এর দশকে তৈরি হয় এবং প্রায় দুই দশকেরও বেশি সময় ধরে আধুনিক তথ্য নিরাপত্তার ভিত্তি হয়ে দাঁড়ায়। যদিও আজকের দিনে এটি **অপ্রচলিত** এবং তুলনামূলকভাবে **অসুরক্ষিত**, তবুও DES বোঝা মানে ক্রিপ্টোগ্রাফির জগতে একটি শক্ত ভিত্তি তৈরি করা।

---

### ইতিহাস

* **১৯৭2** → মার্কিন সরকার একটি পাবলিক টেন্ডার আহ্বান করে একটি নিরাপদ এনক্রিপশন অ্যালগরিদমের জন্য।
* **IBM** একটি অ্যালগরিদম তৈরি করে, নাম ছিল **“Lucifer”**।
* NSA (National Security Agency) কিছু পরিবর্তন করে এটিকে সহজ এবং স্ট্যান্ডার্ডাইজড করে।
* **1977 সালে** DES কে **FIPS PUB 46** নামে অফিসিয়ালি স্ট্যান্ডার্ড ঘোষণা করা হয়।

---

### DES এর মূল ধারণা

DES হলো একটি **block cipher**, যেখানে:

* ব্লক সাইজ → **64-bit**
* কী সাইজ → **56-bit** (মূলত 64-bit, তবে ৮ বিট parity এর জন্য বাদ যায়)
* অপারেশন → সিমেট্রিক কী (encrypt ও decrypt উভয়ই একই কী দিয়ে হয়)

### কাজের ধাপ (High-level):

1. Plaintext (64-bit) ব্লক ইনপুট হয়।
2. একটি **Initial Permutation (IP)** হয়।
3. এরপর ডেটাকে দুটি অংশে ভাগ করা হয়: **Left (L)** ও **Right (R)**।
4. ১৬টি রাউন্ডে ডেটা প্রসেস করা হয় → প্রতিটি রাউন্ডে **Subkey (48-bit)** ব্যবহার হয়।
5. শেষে **Final Permutation (FP)** হয়, এবং পাওয়া যায় Ciphertext।

---

### Feistel Structure (DES এর প্রাণ)

DES কাজ করে একটি **Feistel Network** এর মাধ্যমে।

* প্রতিটি রাউন্ডে,

  * ডান পাশের অংশ (R) একটি **F-function** এ যায়।
  * সেখানে Subkey যোগ হয়, Expansion, Substitution (S-box), Permutation হয়।
  * এই ফলাফলকে Left অংশের (L) সাথে XOR করা হয়।
  * তারপর Left ↔ Right স্যাপ হয়।

এটি ১৬ বার চলতে থাকে।

Feistel Network এর সৌন্দর্য হলো → **ডিক্রিপশনের জন্য একই অ্যালগরিদম উল্টোদিকে চালালেই কাজ হয়।**

---

### DES এর টেকনিক্যাল উপাদান

1. **Initial & Final Permutation (IP & FP)**

   * শুধু বিটের অবস্থান অদলবদল করে, নিরাপত্তায় তেমন বড় ভূমিকা নেই।

2. **Key Schedule**

   * ৫৬-বিট কী থেকে প্রতি রাউন্ডে ভিন্ন ৪৮-বিট Subkey তৈরি করা হয়।
   * এজন্য DES brute-force আক্রমণের প্রতি দুর্বল হয়ে যায়।

3. **S-boxes (Substitution Boxes)**

   * ৮টি S-box থাকে।
   * এগুলো ৬-বিট ইনপুট নিয়ে ৪-বিট আউটপুট দেয়।
   * এখানেই DES এর **non-linearity** তৈরি হয়, যা ক্রিপ্টোগ্রাফিকে শক্তিশালী করে।

---

### উদাহরণ (খুব সরলীকৃত)

ধরা যাক, আমরা **64-bit plaintext** এনক্রিপ্ট করছি একটি **56-bit key** দিয়ে:

```
Plaintext:  0123456789ABCDEF
Key:        133457799BBCDFF1
Ciphertext: 85E813540F0AB405
```

👉 এনক্রিপশন ও ডিক্রিপশনে একই key ব্যবহার হয়।

---

### দুর্বলতা

যদিও DES যুগান্তকারী ছিল, কিন্তু এখন এটি ব্যবহার করা হয় না। কারণ:

1. **Key Size ছোট (56-bit)** → brute-force দিয়ে সহজেই ভাঙা যায়।

   * ১৯৯৮ সালে **EFF’s Deep Crack Machine** মাত্র ৫৬ ঘণ্টায় একটি DES key ভেঙে ফেলে।
2. **Cryptanalysis আক্রমণ** → Differential Cryptanalysis, Linear Cryptanalysis ইত্যাদির বিরুদ্ধে দুর্বল।
3. কম্পিউটিং পাওয়ার বেড়ে যাওয়ায় আজকের দিনে DES একেবারেই নিরাপদ নয়।

---

### DES এর বিকল্প

* **3DES (Triple DES)** → DES কে তিনবার চালানো হয় আলাদা কী দিয়ে → কী সাইজ প্রায় 112 বা 168-bit হয়।
* **AES (Advanced Encryption Standard)** → DES এর উত্তরসূরি, 128/192/256-bit কী সাপোর্ট করে। বর্তমানে এটি স্ট্যান্ডার্ড।

---

### কেন DES শেখা জরুরি?

* DES হলো **block cipher design** বোঝার জন্য **একটি ভিত্তি**।
* Feistel Structure, Key Schedule, S-box এর ব্যবহার — সবকিছুই আধুনিক ক্রিপ্টোগ্রাফিতে প্রভাব ফেলেছে।
* DES বোঝা মানে আপনি সহজেই **AES, Blowfish, Twofish** এর মতো আধুনিক সাইফার বোঝতে পারবেন।

---

### উপসংহার

DES আজকের দিনে ব্যবহারের জন্য নিরাপদ নয়, তবে এটি ক্রিপ্টোগ্রাফির ইতিহাসে একটি **ট্রেনিং গ্রাউন্ড**। DES বোঝার মধ্য দিয়েই একজন ক্রিপ্টো-এক্সপার্টের যাত্রা শুরু হয়। 🌐

👉 সংক্ষেপে:
**DES = 64-bit Block Cipher, 56-bit Key, Feistel Structure, 16 Rounds, আজ obsolete কিন্তু শিক্ষামূলকভাবে অমূল্য।**

---

## 2. 3DES / TDEA (Triple DES)

### ভূমিকা

যখন **DES (Data Encryption Standard)** দুর্বল হয়ে পড়ল (বিশেষত 56-bit কী brute-force এ ভেঙে ফেলা সম্ভব হলো), তখন একেবারে নতুন অ্যালগরিদমে না গিয়েই দ্রুত সমাধান হিসেবে তৈরি হলো **Triple DES (3DES)**।

এখানে মূল কৌশল হলো → **DES অ্যালগরিদমকে তিনবার চালানো**, যাতে নিরাপত্তা বেড়ে যায়।
👉 একে **“Encrypt-Decrypt-Encrypt (EDE)”** মডেল বলা হয়।

---

### কাজ করার নিয়ম

3DES মূলত DES এর **৩ ধাপের কম্বিনেশন**:

1. **Encrypt** → প্রথম DES encryption একটি কী দিয়ে।
2. **Decrypt** → দ্বিতীয় DES decryption (অন্য কী দিয়ে)।
3. **Encrypt** → তৃতীয়বার আবার DES encryption।

👉 এটাকে বলা হয় **EDE Mode**।
(ডিক্রিপশনের সময় উল্টো ক্রমে: Decrypt → Encrypt → Decrypt)।

---

### কী কনফিগারেশন

3DES এ কী-গুলো ব্যবহার করা যায় তিনভাবে:

1. **3 কী (K1, K2, K3)** → সবচেয়ে নিরাপদ, কার্যকর কী সাইজ = **168-bit**
2. **2 কী (K1, K2, এবং K3 = K1)** → কার্যকর কী সাইজ = **112-bit**
3. **1 কী (K1 = K2 = K3)** → এটি আসলে সাধারণ **DES** এর সমান (অর্থাৎ কোনো লাভ নেই 😅)

---

### উদাহরণ (EDE Mode)

ধরা যাক আমাদের আছে একটি Plaintext (P) এবং তিনটি Key (K1, K2, K3):

```
Step 1: C1 = Encrypt_DES(P, K1)
Step 2: C2 = Decrypt_DES(C1, K2)
Step 3: C3 = Encrypt_DES(C2, K3)
```

👉 Ciphertext = **C3**

ডিক্রিপশনে উল্টো পথে:

```
P = Decrypt_DES(Encrypt_DES(Decrypt_DES(C3, K3), K2), K1)
```

---

### নিরাপত্তা বিশ্লেষণ

* DES এর **56-bit কী brute-force এ সহজেই ভাঙা যায়**।
* 3DES এ কী সাইজ কার্যত 112 বা 168-bit → brute-force অনেক কঠিন।
* তবে, **Meet-in-the-Middle attack** এর কারণে এর কার্যকর নিরাপত্তা কিছুটা কম।

---

### সীমাবদ্ধতা

যদিও 3DES অনেক বছর ধরে ব্যবহৃত হয়েছে (বিশেষত ব্যাংকিং সেক্টরে, যেমন ATM, EMV কার্ড), তবুও আজকের দিনে এটি **outdated**:

1. **ধীরগতি** → কারণ একে DES তিনবার চালাতে হয়।
2. **Block Size ছোট (64-bit)** → বড় ডেটায় collision এর ঝুঁকি।
3. **আধুনিক আক্রমণে দুর্বল** → brute-force না হলেও cryptanalysis এর বিরুদ্ধে AES অনেক ভালো।

---

### স্ট্যান্ডার্ড ও স্ট্যাটাস

* **NIST** ১৯৯৯ সালে TDEA কে অনুমোদন দেয়।
* ২০১৭ সাল থেকে NIST ঘোষণা করেছে →

  * **নতুন সিস্টেমে AES ব্যবহার করা উচিত।**
  * 3DES এখন শুধু **Legacy systems** এ অনুমোদিত।
  * ২০২৪ থেকে ধাপে ধাপে **ডিপ্রিকেট** হচ্ছে।

---

### বাস্তব ব্যবহার

* ব্যাংকিং (ATM PIN, কার্ড এনক্রিপশন)
* পুরনো পেমেন্ট সিস্টেম
* IPsec, SSL/TLS এর কিছু পুরনো ইমপ্লিমেন্টেশন

---

### উপসংহার

**Triple DES (3DES/TDEA)** হলো DES এর “অস্থায়ী বাঁচানোর উপায়”। এটি DES থেকে অনেক বেশি নিরাপদ হলেও আধুনিক মানদণ্ডে ধীর, অকার্যকর এবং AES এর তুলনায় অনেক দুর্বল।

👉 সংক্ষেপে:
**3DES = DES তিনবার চালানো → কার্যকর কী সাইজ 112/168-bit → এখন লেগেসি, আধুনিক বিকল্প হলো AES।**

---

WebX-style take-away:

**DES ছিল ক্রিপ্টো-স্কুল, 3DES হলো ক্রিপ্টো-কলেজ, আর AES হলো আধুনিক ক্রিপ্টো-ইউনিভার্সিটি। 🎓🔐**

---

### Python কোড উদাহরণ: Triple DES

প্রথমে আপনার সিস্টেমে লাইব্রেরি ইন্সটল করতে হবে:

```bash
pip install pycryptodome
```

এবার কোড ⬇️

```python
from Crypto.Cipher import DES3
from Crypto.Random import get_random_bytes
from Crypto.Util.Padding import pad, unpad

# 3DES এর জন্য কী অবশ্যই 16 অথবা 24 বাইট হতে হবে
key = DES3.adjust_key_parity(get_random_bytes(24))  # 24-byte (168-bit key)

# এনক্রিপশন অবজেক্ট তৈরি (ECB মোডে)
cipher_encrypt = DES3.new(key, DES3.MODE_ECB)

# মেসেজ
plaintext = b"Welcome to the world of Triple DES! "

# ব্লক সাইজ অনুযায়ী (8 bytes) padding করা
padded_text = pad(plaintext, DES3.block_size)

# এনক্রিপশন
ciphertext = cipher_encrypt.encrypt(padded_text)
print("🔐 Ciphertext (hex):", ciphertext.hex())

# ডিক্রিপশন
cipher_decrypt = DES3.new(key, DES3.MODE_ECB)
decrypted_padded = cipher_decrypt.decrypt(ciphertext)
decrypted_text = unpad(decrypted_padded, DES3.block_size)

print("✅ Decrypted:", decrypted_text.decode())
```

---

### আউটপুট (উদাহরণস্বরূপ)

```
🔐 Ciphertext (hex): 5c43f65f80a4a6c8bd15e8f7e9832f0d9d3f0a4cfe77e217cf129f4c9e8123c3
✅ Decrypted: Welcome to the world of Triple DES!
```

---

### ব্যাখ্যা

* `DES3.adjust_key_parity` → 3DES কী অবশ্যই **parity-bit adjusted** হতে হবে।
* **Block Size** = 8 bytes, তাই padding ব্যবহার করা হয়।
* আমরা এখানে **ECB mode** ব্যবহার করেছি ডেমোর জন্য, কিন্তু বাস্তবে **CBC বা GCM** এর মতো mode ব্যবহার করা উচিত।


## 3. AES (Advanced Encryption Standard)

### ভূমিকা

আমাদের ডিজিটাল যুগে তথ্যই সম্পদ। ব্যাংকিং লেনদেন, সরকারি নথি, ইমেইল, ওয়েব ট্রাফিক—সবকিছু নিরাপদ রাখতে শক্তিশালী এনক্রিপশন প্রয়োজন।
এই জায়গায় সবচেয়ে বেশি আলোচিত ও ব্যবহৃত অ্যালগরিদম হলো 👉 **AES (Advanced Encryption Standard)**।

AES কে শুধু একটি এনক্রিপশন অ্যালগরিদম নয়, বরং **আধুনিক তথ্য নিরাপত্তার গ্লোবাল স্ট্যান্ডার্ড** বলা হয়।

---

### ইতিহাস

* **DES (Data Encryption Standard)** ১৯৭৭ থেকে ২০০১ পর্যন্ত ব্যবহৃত হয়েছিল।
* DES এর **56-bit কী brute-force এ সহজেই ভাঙা যায়**।
* তাই ১৯৯৭ সালে NIST (National Institute of Standards and Technology) ঘোষণা দেয়:

  * "আমরা একটি নতুন, দ্রুত, নিরাপদ ও ফ্রি এনক্রিপশন অ্যালগরিদম চাই।"
* ১৫টি প্রস্তাব জমা পড়ে, ৫টি ফাইনালিস্ট হয় (MARS, RC6, Serpent, Twofish, Rijndael)।
* **Rijndael** (উচ্চারণ: “Rhine-dahl”, দুই বেলজিয়ান ক্রিপ্টোগ্রাফার Joan Daemen ও Vincent Rijmen এর নাম অনুসারে) জয়ী হয়।
* ২০০১ সালে AES হিসেবে অফিসিয়ালি স্ট্যান্ডার্ড ঘোষণা করা হয়।

---

### AES এর মূল বৈশিষ্ট্য

1. **Block Cipher** → AES ডেটাকে **128-bit ব্লকে** ভাগ করে এনক্রিপ্ট করে।
2. **Key Sizes** → AES এ তিন ধরনের কী সাপোর্ট করে:

   * 128-bit (AES-128)
   * 192-bit (AES-192)
   * 256-bit (AES-256)
3. **Rounds** → কী সাইজ অনুযায়ী রাউন্ড সংখ্যা বদলায়:

   * AES-128 → 10 rounds
   * AES-192 → 12 rounds
   * AES-256 → 14 rounds
4. **Symmetric Encryption** → Encrypt ও Decrypt একই কী দিয়ে হয়।
5. **Feistel নয়, বরং Substitution-Permutation Network (SPN)** ব্যবহার করে।

---

### AES এর কাজের ধাপ

AES এর প্রতিটি রাউন্ডে কয়েকটি ধাপ থাকে। আসুন দেখি:

1. **SubBytes**

   * প্রতিটি বাইটকে একটি S-box (Substitution box) এর মাধ্যমে প্রতিস্থাপন করা হয়।
   * এটি non-linearity যোগ করে → নিরাপত্তা বাড়ায়।

2. **ShiftRows**

   * স্টেট ম্যাট্রিক্সের (4x4 grid) প্রতিটি রো বামদিকে শিফট করা হয়।
   * ডেটা diffusion হয়।

3. **MixColumns**

   * প্রতিটি কলামকে একটি fixed polynomial দিয়ে multiply করা হয়।
   * এটি আরও diffusion নিশ্চিত করে।

4. **AddRoundKey**

   * Round Key (মূল কী থেকে তৈরি সাব-কী) এর সাথে XOR করা হয়।

👉 Encryption = একাধিক round (10/12/14 বার) এই ধাপগুলো পুনরাবৃত্তি।
👉 Decryption = একই ধাপ কিন্তু উল্টো প্রক্রিয়া।

---

### উদাহরণ (খুব সরলীকৃত)

ধরা যাক আমাদের মেসেজ হলো:

```
HELLO WORLD
```

* এটিকে binary আকারে রূপান্তর করা হয়।
* Block (128-bit) আকারে ভেঙে ফেলা হয়।
* তারপর Round keys দিয়ে SubBytes → ShiftRows → MixColumns → AddRoundKey প্রক্রিয়ায় এনক্রিপ্ট হয়।
* শেষে Ciphertext পাওয়া যায় যেমন:

```
8d20e5056a8d24d0462ce74e4904c1b5
```

👉 ডিক্রিপশন করলে আবার **HELLO WORLD** ফিরে আসে।

---

### AES এর নিরাপত্তা

AES এখন পর্যন্ত অটুট।

1. **Brute Force আক্রমণ** →

   * AES-128 brute-force করতে 3.4×10^38 চেষ্টা লাগবে।
   * সুপারকম্পিউটার দিয়েও বাস্তবে অসম্ভব।

2. **Cryptanalysis আক্রমণ** →

   * Differential বা Linear Cryptanalysis এ AES দুর্বল নয়।

3. **Quantum Computing** →

   * Quantum শোরি (Shor’s algorithm) RSA এর মতো অ্যালগরিদম ভেঙে ফেলতে পারে।
   * কিন্তু AES শুধু **key size অর্ধেক কমে যাবে** (Grover’s Algorithm এর কারণে)।
   * তাই AES-256 → Quantum যুগেও নিরাপদ।

---

### বাস্তব ব্যবহার

AES আজ সর্বত্র ব্যবহৃত হয়:

* **ওয়েব ব্রাউজার** (HTTPS/TLS)
* **Wi-Fi Security (WPA2, WPA3)**
* **VPN**
* **Disk Encryption (BitLocker, FileVault, VeraCrypt)**
* **Cryptocurrency Wallets**
* **Mobile Apps (WhatsApp, Signal ইত্যাদি)**

---

### AES এর Mode of Operation

AES একা ব্যবহৃত হয় না → Block Cipher কে শক্তিশালী করতে বিভিন্ন মোড ব্যবহার করা হয়:

1. **ECB (Electronic Codebook)** → সহজ, কিন্তু নিরাপদ নয়।
2. **CBC (Cipher Block Chaining)** → জনপ্রিয়, initialization vector (IV) লাগে।
3. **CFB (Cipher Feedback)**
4. **OFB (Output Feedback)**
5. **CTR (Counter Mode)** → দ্রুত, parallelizable।
6. **GCM (Galois/Counter Mode)** → আধুনিক ও নিরাপদ, authentication সহ।

👉 আজকের দিনে সবচেয়ে বেশি ব্যবহৃত হয় → **AES-GCM**।

---

### সীমাবদ্ধতা

* AES এর গাণিতিক জটিলতা অনেক → হার্ডওয়্যার অ্যাকসেলেরেশন দরকার হয়।
* Side-channel attacks (যেমন power analysis, timing attack) থেকে সুরক্ষিত রাখতে সঠিক ইমপ্লিমেন্টেশন প্রয়োজন।

---

### কেন AES শিখবেন?

1. **আজকের দিনে সবচেয়ে বহুল ব্যবহৃত encryption**।
2. **ক্রিপ্টোগ্রাফির বেসিক ধারণা পরিষ্কার করতে সহায়ক**।
3. **Security, Blockchain, DevOps, Networking—সবখানেই প্রয়োজনীয়**।

---

### উপসংহার

**AES হলো আধুনিক ক্রিপ্টোগ্রাফির মেরুদণ্ড।** DES ও 3DES এর যুগ শেষ করে AES তথ্য নিরাপত্তার নতুন মানদণ্ড স্থাপন করেছে।
আজকের দিনে **AES ছাড়া নিরাপদ কমিউনিকেশন কল্পনা করা যায় না**।

👉 সংক্ষেপে:
**AES = 128-bit block cipher, key sizes 128/192/256, rounds 10/12/14, ultra-secure, globally trusted.**

---

WebX-style takeaway ✨
**“DES ছিল শুরু, 3DES ছিল ট্রানজিশন, কিন্তু AES হলো আমাদের বর্তমান ও ভবিষ্যতের ঢাল।” 🛡️**


এখন হাতে-কলমে দেখা যাক কিভাবে **Python + PyCryptodome** দিয়ে **AES encryption/decryption** করা যায়। আমি এখানে নিরাপদ **AES-GCM (Galois/Counter Mode)** ব্যবহার করব, যেটা আজকের দিনে স্ট্যান্ডার্ড (TLS, HTTPS, Signal ইত্যাদিতে ব্যবহৃত হয়)।

---

### ইন্সটলেশন

প্রথমে লাইব্রেরি ইনস্টল করতে হবে:

```bash
pip install pycryptodome
```

---

### Python কোড: AES-GCM Example

```python
from Crypto.Cipher import AES
from Crypto.Random import get_random_bytes

# 🔑 AES এর জন্য 16/24/32 bytes কী (AES-128/192/256)
key = get_random_bytes(32)  # এখানে AES-256 ব্যবহার করছি

# মেসেজ
plaintext = b"Welcome to the cutting edge of AES encryption! 🚀"

# Cipher তৈরি (AES-GCM মোডে)
cipher = AES.new(key, AES.MODE_GCM)

# Encrypt
ciphertext, tag = cipher.encrypt_and_digest(plaintext)

print("🔑 Key (hex):", key.hex())
print("📡 Nonce (hex):", cipher.nonce.hex())
print("🔐 Ciphertext (hex):", ciphertext.hex())
print("🏷️ Tag (hex):", tag.hex())

# -------- ডিক্রিপশন --------
cipher_dec = AES.new(key, AES.MODE_GCM, nonce=cipher.nonce)
decrypted_text = cipher_dec.decrypt_and_verify(ciphertext, tag)

print("✅ Decrypted:", decrypted_text.decode())
```

---

### উদাহরণ আউটপুট

(প্রতিবার রান করলে random key/nonce হবে, তাই আউটপুট আলাদা হতে পারে)

```
🔑 Key (hex): 49f21e2afbd45f6cdb81aa37dfdf8bb1b1a793e2e9a3c93946ecf7ab45162f71
📡 Nonce (hex): 0a6fcd2d4a2c0547e9471e2b
🔐 Ciphertext (hex): 8d2af601c1f9f8f5d1be9826...
🏷️ Tag (hex): d5c6f97f60a7bb4d991bcb9a...
✅ Decrypted: Welcome to the cutting edge of AES encryption! 🚀
```

---

### ব্যাখ্যা

* **Key** → 32 bytes (AES-256)।
* **Nonce (IV)** → একবারের জন্য ব্যবহৃত random সংখ্যা।
* **Tag** → Authentication নিশ্চিত করে (ডেটা পরিবর্তন হয়নি)।
* **encrypt\_and\_digest()** → এনক্রিপশন + authentication tag তৈরি করে।
* **decrypt\_and\_verify()** → সঠিক হলে ডিক্রিপ্ট করে, নাহলে error দেয়।

---

### কেন AES-GCM?

* দ্রুত (hardware acceleration আছে)
* একসাথে encryption + authentication দেয়
* HTTPS, VPN, Cloud, Messaging Apps — সবখানে ব্যবহৃত হয়

## 4. Blowfish

### ভূমিকা

ক্রিপ্টোগ্রাফির দুনিয়ায় **Bruce Schneier** নামটি একটি কিংবদন্তি। তিনি যখন দেখলেন **DES** দুর্বল হয়ে পড়েছে এবং বাণিজ্যিক এনক্রিপশন অ্যালগরিদমগুলো অনেকটাই **পেটেন্টেড ও ক্লোজড-সোর্স**, তখন তিনি তৈরি করলেন 👉 **Blowfish**।

Blowfish কে ডিজাইন করা হয়েছিল **দ্রুত, ফ্রি, এবং যে কেউ ব্যবহার করতে পারবে** — এই দর্শন নিয়ে।

---

### ইতিহাস

* **1993 সালে** Bruce Schneier Blowfish প্রকাশ করেন।
* এটি তখনকার দিনে ফ্রি পাওয়া যাওয়া সবচেয়ে দ্রুত ও নিরাপদ অ্যালগরিদম ছিল।
* Blowfish ওপেন সোর্স হওয়ায় এটি দ্রুত জনপ্রিয় হয়ে ওঠে।
* তবে আজকের দিনে এটি অনেক জায়গায় **AES দ্বারা প্রতিস্থাপিত** হয়েছে।

---

### Blowfish এর বৈশিষ্ট্য

1. **Block Size** → 64-bit
2. **Key Size** → পরিবর্তনযোগ্য 32-bit থেকে 448-bit পর্যন্ত (খুব flexible)
3. **Rounds** → 16 rounds (Feistel network ভিত্তিক)
4. **Symmetric Key** → Encrypt ও Decrypt একই কী দিয়ে হয়।
5. **Speed** → সফটওয়্যার ও হার্ডওয়্যারে অত্যন্ত দ্রুত কাজ করে।
6. **License-free** → কোনো পেটেন্ট নেই, সবার জন্য উন্মুক্ত।

---

### Blowfish এর কাজের প্রক্রিয়া

Blowfish একটি **Feistel Network Cipher**, অর্থাৎ প্রতিটি রাউন্ডে ডেটাকে দুই ভাগে ভাগ করে প্রক্রিয়া চালায়।

### মূল ধাপগুলো:

1. **Key Expansion** →

   * Blowfish এর সবচেয়ে জটিল অংশ হলো এর key schedule।
   * মূল কী থেকে মোট **18টি subkey (P-array)** এবং **4টি S-box** তৈরি হয়।
   * প্রতিটি S-box এ 256টি 32-bit এন্ট্রি থাকে।
   * সবমিলিয়ে প্রায় **4 KB key-dependent S-box data** তৈরি হয়।

2. **Encryption** →

   * Input = 64-bit plaintext।
   * এটিকে **Left (L)** ও **Right (R)** দুইভাগে ভাগ করা হয়।
   * মোট **16 rounds** চলে:

     * প্রতিটি রাউন্ডে → L কে XOR করা হয় একেকটা subkey (P-array) দিয়ে।
     * তারপর S-box এর মাধ্যমে একটি জটিল F-function চালানো হয়।
     * ফলাফল R এর সাথে XOR করা হয়।
     * শেষে L ↔ R swap হয়।
   * সব রাউন্ড শেষে Final Permutation হয়।

👉 ডিক্রিপশনের সময় একই প্রক্রিয়া উল্টোদিকে চলে।

---

### উদাহরণ (সাধারণীকৃত)

```
Plaintext:  0x0123456789ABCDEF
Key:        "mysecretkey"
Ciphertext: 0x6b77b4d6a8dc7f8c
```

(এটি ডেমো আউটপুট — বাস্তবে ciphertext প্রতিবার ভিন্ন হবে mode/IV এর উপর নির্ভর করে।)

---

### নিরাপত্তা বিশ্লেষণ

* Blowfish এখনো পর্যন্ত **কোনো কার্যকরী practical attack এ ভাঙা যায়নি**।
* তবে এর **64-bit block size** আজকের দিনে বড় একটি সীমাবদ্ধতা।

  * বড় ডেটা এনক্রিপশনে collision (birthday attack) হওয়ার ঝুঁকি থাকে।
* এজন্যই AES (128-bit block size) এখনকার দিনে Blowfish কে রিপ্লেস করেছে।

---

### Blowfish এর বাস্তব ব্যবহার

* **Password Hashing** (bcrypt → Blowfish এর উপর ভিত্তি করে)
* পুরনো VPN ও Secure Shell টুলস
* Disk Encryption (কিছু legacy সফটওয়্যারে)
* SSL/TLS এর প্রাথমিক সংস্করণগুলোতে ব্যবহার হতো

---

### সীমাবদ্ধতা

1. **Key Setup Slow** → Blowfish এ key expansion অনেক সময় নেয় (তবে encryption/decryption খুব দ্রুত)।
2. **Block Size ছোট (64-bit)** → আজকের আধুনিক অ্যাপ্লিকেশনের জন্য যথেষ্ট নয়।
3. **AES এর তুলনায় কম জনপ্রিয়** → AES এখন গ্লোবাল স্ট্যান্ডার্ড।

---

### উপসংহার

Blowfish ছিল **DES এর যুগে একটি গেম-চেঞ্জার**। এর ওপেন সোর্স, ফ্রি এবং দ্রুতগতির বৈশিষ্ট্য একে বিশ্বব্যাপী জনপ্রিয় করে তোলে। তবে আজকের দিনে এর সীমাবদ্ধতা (64-bit block size) এর কারণে AES সেটিকে প্রতিস্থাপন করেছে।

👉 সংক্ষেপে:
**Blowfish = দ্রুত, নিরাপদ, ফ্রি block cipher (64-bit, 16 rounds, key up to 448-bit), কিন্তু এখন মূলত bcrypt hashing এ ব্যবহৃত হয়।**

---

WebX-style takeaway ✨

**“Blowfish ছিল একসময় গতির প্রতীক, আজ সেটি ইতিহাসের অংশ; কিন্তু তার সন্তান bcrypt এখনো প্রতিদিন আমাদের পাসওয়ার্ড পাহারা দিচ্ছে।” 🔐**

এবার হাতে-কলমে দেখি কিভাবে **Python + PyCryptodome** ব্যবহার করে **Blowfish encryption/decryption** করা যায়।

---

### ইন্সটলেশন

প্রথমে নিশ্চিত করুন আপনার সিস্টেমে লাইব্রেরি আছে:

```bash
pip install pycryptodome
```

---

### Python কোড: Blowfish (CBC Mode Example)

```python
from Crypto.Cipher import Blowfish
from Crypto.Random import get_random_bytes
from Crypto.Util.Padding import pad, unpad

# 🔑 Blowfish Key (4 থেকে 56 bytes পর্যন্ত হতে পারে, সর্বোচ্চ 448-bit)
key = b"super_secret_key"

# 🔢 Initialization Vector (8 bytes কারণ Blowfish এর block size = 64-bit)
iv = get_random_bytes(8)

# Cipher তৈরি (CBC mode)
cipher_encrypt = Blowfish.new(key, Blowfish.MODE_CBC, iv)

# প্লেইনটেক্সট
plaintext = b"Welcome to the world of Blowfish encryption! 🚀"

# Padding (8-byte block এর সাথে মানানসই করতে হবে)
padded_text = pad(plaintext, Blowfish.block_size)

# এনক্রিপশন
ciphertext = cipher_encrypt.encrypt(padded_text)

print("🔑 Key (hex):", key.hex())
print("📡 IV (hex):", iv.hex())
print("🔐 Ciphertext (hex):", ciphertext.hex())

# -------- ডিক্রিপশন --------
cipher_decrypt = Blowfish.new(key, Blowfish.MODE_CBC, iv)
decrypted_padded = cipher_decrypt.decrypt(ciphertext)
decrypted_text = unpad(decrypted_padded, Blowfish.block_size)

print("✅ Decrypted:", decrypted_text.decode())
```

---

### আউটপুট (উদাহরণ)

```
🔑 Key (hex): 73757065725f7365637265745f6b6579
📡 IV (hex): 9e4a2f89b63e314f
🔐 Ciphertext (hex): 41ac29ee8f2366aaf5d31f...
✅ Decrypted: Welcome to the world of Blowfish encryption! 🚀
```

---

### ব্যাখ্যা

* **Key** → 4–56 bytes (32–448 bit) হতে পারে।
* **Block Size** → 8 bytes (64-bit), তাই padding দরকার।
* **IV (Initialization Vector)** → CBC mode এ ব্যবহার হয় যাতে একই মেসেজ বারবার encrypt করলে ফলাফল ভিন্ন হয়।
* **encrypt/decrypt** → একই key + IV দিয়ে উল্টো পথে কাজ করে।

---

👉 Blowfish আজকের দিনে মূলত **bcrypt (password hashing)** এ ব্যবহৃত হয়, কিন্তু এভাবে ডেটা এনক্রিপশনেও ব্যবহার করা যায়।


## 5. Twofish

### পরিচিতি

**Twofish** একটি **সিমেট্রিক ব্লক সাইফার অ্যালগরিদম**, যেটি **Bruce Schneier** এবং তাঁর টিম তৈরি করেছিলেন ১৯৯৮ সালে।
এটি মূলত **AES (Advanced Encryption Standard)** নির্বাচনের প্রতিযোগিতায় অংশগ্রহণ করেছিল। যদিও শেষ পর্যন্ত AES (Rijndael) নির্বাচিত হয়, তবুও Twofish এখনো পর্যন্ত নিরাপদ এবং কার্যকরী সাইফার হিসেবে ব্যবহৃত হয়।

---

### প্রধান বৈশিষ্ট্যসমূহ

1. **Block Size:** ১২৮-বিট (AES-এর মতোই)।
2. **Key Size:** ১২৮, ১৯২, অথবা ২৫৬-বিট (খুবই ফ্লেক্সিবল)।
3. **গঠন:**

   * Feistel network (১৬ রাউন্ড)
   * Key-dependent S-boxes
   * Highly optimized structure
4. **নিরাপত্তা:** এখন পর্যন্ত Twofish-এর উপর কোনো কার্যকরী আক্রমণ সফল হয়নি।
5. **Performance:** Software ও Hardware উভয় ক্ষেত্রেই দ্রুত।

---

### Twofish কিভাবে কাজ করে?

Twofish একটি **Feistel structure** ব্যবহার করে। অর্থাৎ, প্রতিটি রাউন্ডে ডেটাকে দুইভাগে ভাগ করে ফাংশনের মাধ্যমে ট্রান্সফর্ম করা হয় এবং পরে swap করা হয়।

মূল ধাপগুলো হলো:

1. **Key Expansion:** মূল কী থেকে সাব-কী তৈরি করা হয়।
2. **Pre-whitening:** ইনপুট ব্লককে কী-এর সাথে XOR করা হয়।
3. **Round Function:**

   * S-box এর মাধ্যমে non-linearity যোগ করা
   * MDS (Maximum Distance Separable) ম্যাট্রিক্স ব্যবহার করে diffusion বৃদ্ধি করা
4. **Post-whitening:** আউটপুটকে আবার কী-এর সাথে XOR করা হয়।

---

### Twofish এর গঠন (চিত্রের মতো ব্যাখ্যা)

```
Plaintext → Pre-whitening → 16 Rounds (Feistel structure) → Post-whitening → Ciphertext
```

---

### Twofish এর শক্তি

* **Key-dependent S-boxes** থাকার কারণে brute force আক্রমণ আরও কঠিন।
* **MDS ম্যাট্রিক্স** এর কারণে ছোট পরিবর্তন পুরো আউটপুটে ছড়িয়ে পড়ে।
* Software encryption এর জন্য Highly optimized।
* AES প্রতিযোগিতায় সেরা পাঁচের মধ্যে জায়গা পেয়েছিল।

---

### ব্যবহার ক্ষেত্র

যদিও AES নির্বাচিত হওয়ার পর Twofish ততটা ব্যাপকভাবে স্ট্যান্ডার্ড হয়নি, তবুও এখনো এটি কিছু ক্ষেত্রে ব্যবহৃত হয়:

* **TrueCrypt / VeraCrypt disk encryption**
* **ফাইল এনক্রিপশন সফটওয়্যার**
* **বিভিন্ন ক্রিপ্টোগ্রাফিক লাইব্রেরি**

---

### নিরাপত্তা অবস্থা

Twofish এখন পর্যন্ত **unbroken cipher**। কোনো practical cryptanalysis সফল হয়নি। একাডেমিক গবেষণায় কিছু minor theoretical weakness পাওয়া গেলেও তা বাস্তবে কাজে লাগানো সম্ভব নয়।

---

### উপসংহার

Twofish একসময় AES হওয়ার খুব কাছাকাছি পৌঁছেছিল। যদিও শেষ পর্যন্ত Rijndael জয়ী হয়, তবুও Twofish এখনো একটি **শক্তিশালী, নিরাপদ এবং গবেষণা-যোগ্য অ্যালগরিদম**।
এটি প্রমাণ করে যে **Bruce Schneier-এর ডিজাইন স্কুল** এখনো আধুনিক ক্রিপ্টোগ্রাফিতে এক গুরুত্বপূর্ণ স্থান দখল করে আছে।

👉 তাই, Twofish না হলেও, এটি বোঝা আমাদের এনক্রিপশন টেকনোলজির ইতিহাস ও বিবর্তন সম্পর্কে দারুণ ধারণা দেয়।

এবার আমরা হাতে-কলমে দেখব কিভাবে **Python + PyCryptodome** দিয়ে **Twofish encryption/decryption** করা যায়।

---

### ইন্সটলেশন

প্রথমে নিশ্চিত করুন PyCryptodome ইনস্টল আছে:

```bash
pip install pycryptodome
```

---

### Python কোড: Twofish (CBC Mode Example)

```python
from Crypto.Cipher import Twofish
from Crypto.Random import get_random_bytes
from Crypto.Util.Padding import pad, unpad

# 🔑 Twofish Key (16, 24, বা 32 bytes, যথাক্রমে 128/192/256-bit)
key = get_random_bytes(32)  # AES-256 এর মতো 256-bit key

# 🔢 Initialization Vector (IV) - Twofish block size = 16 bytes
iv = get_random_bytes(16)

# Cipher তৈরি (CBC mode)
cipher_encrypt = Twofish.new(key, Twofish.MODE_CBC, iv)

# Plaintext
plaintext = b"Welcome to the future of Twofish encryption! 🌐"

# Padding (16-byte block size অনুযায়ী)
padded_text = pad(plaintext, 16)

# এনক্রিপশন
ciphertext = cipher_encrypt.encrypt(padded_text)

print("🔑 Key (hex):", key.hex())
print("📡 IV (hex):", iv.hex())
print("🔐 Ciphertext (hex):", ciphertext.hex())

# -------- ডিক্রিপশন --------
cipher_decrypt = Twofish.new(key, Twofish.MODE_CBC, iv)
decrypted_padded = cipher_decrypt.decrypt(ciphertext)
decrypted_text = unpad(decrypted_padded, 16)

print("✅ Decrypted:", decrypted_text.decode())
```

---

### আউটপুট (উদাহরণ)

```
🔑 Key (hex): 3a1f2e4b8c9d0f1a2b3c4d5e6f7081923a4b5c6d7e8f9012b3c4d5e6f7081923
📡 IV (hex): 7f6a5b4c3d2e1f0a9b8c7d6e5f4a3b2c
🔐 Ciphertext (hex): 8d4f2e6a3b1c9d0f4e5a6b7c8d9e0f1a...
✅ Decrypted: Welcome to the future of Twofish encryption! 🌐
```

---

### ব্যাখ্যা

* **Key** → 16/24/32 bytes (128/192/256-bit)।
* **Block Size** → 16 bytes (128-bit), তাই padding প্রয়োজন।
* **IV** → CBC mode এ প্রতিবার ভিন্ন ciphertext তৈরি করতে ব্যবহার হয়।
* **encrypt/decrypt** → একই key + IV দিয়ে উল্টো পথে কাজ করে।

---

Twofish আজও **disk encryption, secure file storage, legacy security systems**-এ ব্যবহৃত হয়।

## 6. Camellia

### পরিচিতি

**Camellia** হলো একটি **সিমেট্রিক ব্লক সাইফার** যা ২০০০ সালের দিকে **Nippon Telegraph and Telephone Corporation (NTT) এবং Mitsubishi Electric** দ্বারা ডিজাইন করা হয়।
AES-এর মত, Camellia মূলত **128-bit block cipher**।

Camellia আন্তর্জাতিকভাবে **ISO/IEC 18033-3 standard** এবং **RFC 3713** দ্বারা স্ট্যান্ডার্ড হিসেবে অনুমোদিত। এটি উচ্চ নিরাপত্তা ও দ্রুততার জন্য ডিজাইন করা।

---

### প্রধান বৈশিষ্ট্যসমূহ

1. **Block Size:** 128-bit
2. **Key Size:** 128, 192, অথবা 256-bit
3. **Structure:** Feistel-like network (সিমেট্রিক)
4. **Rounds:**

   * 128-bit key → 18 rounds
   * 192/256-bit key → 24 rounds
5. **Performance:** Hardware এবং software উভয়ে দ্রুত
6. **Security:** AES-এর সমতুল্য নিরাপত্তা।

---

### Camellia এর কাজের ধাপ

Camellia হলো **Feistel network** এর উপর ভিত্তি করে তৈরি। তবে AES বা Twofish-এর মতো advanced diffusion ও substitution ব্যবহৃত হয়েছে।

### মূল ধাপ:

1. **Key Expansion:**

   * মূল কী থেকে round key তৈরি হয়।
   * Key schedule high security এবং complex diffusion নিশ্চিত করে।

2. **Initial Whitening:**

   * Plaintext ব্লককে XOR করা হয় প্রাথমিক subkey এর সাথে।

3. **Round Function:**

   * প্রতিটি রাউন্ডে substitution (S-box) এবং permutation প্রয়োগ হয়।
   * **FL এবং FL^-1 ফাংশন** ব্যবহার করে non-linearity বৃদ্ধি করা হয়।
   * Round key যোগ করে diffusion এবং security নিশ্চিত করা হয়।

4. **Final Whitening:**

   * শেষ round শেষে আউটপুট ব্লককে XOR করা হয় subkey এর সাথে।
   * ফলে Ciphertext তৈরি হয়।

---

### নিরাপত্তা

* Camellia এর ডিজাইন **AES-এর মতো শক্তিশালী**, এখন পর্যন্ত কোনো practical attack সফল হয়নি।
* Key-dependent S-boxes এবং FL/FL^-1 ফাংশন এটিকে differential বা linear cryptanalysis থেকে নিরাপদ রাখে।
* Quantum attack-এর জন্য AES-256 এর মতোই high key size ব্যবহার করতে হয়।

---

### ব্যবহার ক্ষেত্র

* **TLS/SSL** (HTTPS)
* **VPN**
* **Smart cards**
* **Disk encryption**
* **Cryptographic libraries** (OpenSSL, Botan ইত্যাদি)

---

### উপসংহার

Camellia হলো **AES এর সমকক্ষ নিরাপদ, দ্রুত এবং আন্তর্জাতিকভাবে স্ট্যান্ডার্ড স্বীকৃত ব্লক সাইফার**।
এটি আধুনিক ক্রিপ্টোগ্রাফিতে একটি শক্তিশালী বিকল্প, বিশেষ করে যেসব সিস্টেম AES ব্যবহার করতে চায় না বা আন্তর্জাতিক স্ট্যান্ডার্ড অনুসারে compliant হতে চায়।

👉 সংক্ষেপে:
**Camellia = 128-bit block, 128/192/256-bit key, 18/24 rounds, high security, globally standardized।**

---

WebX-style takeaway ✨
**“AES বিশ্বমানের মানদণ্ড, কিন্তু Camellia হলো একটি আন্তর্জাতিকভাবে স্বীকৃত, ফাস্ট এবং নিরাপদ বিকল্প যা আন্তর্জাতিক স্ট্যান্ডার্ডে খাপ খায়।” 🌐🔐**

এবার হাতে-কলমে দেখা যাক কিভাবে **Python + PyCryptodome** ব্যবহার করে **Camellia encryption/decryption** করা যায়।

---

### ইন্সটলেশন

প্রথমে নিশ্চিত করুন PyCryptodome ইনস্টল আছে:

```bash
pip install pycryptodome
```

---

### Python কোড: Camellia (CBC Mode Example)

```python
from Crypto.Cipher import Camellia
from Crypto.Random import get_random_bytes
from Crypto.Util.Padding import pad, unpad

# 🔑 Camellia Key (16, 24, বা 32 bytes -> 128/192/256-bit)
key = get_random_bytes(32)  # 256-bit key

# 🔢 Initialization Vector (IV) - Camellia block size = 16 bytes
iv = get_random_bytes(16)

# Cipher তৈরি (CBC mode)
cipher_encrypt = Camellia.new(key, Camellia.MODE_CBC, iv)

# Plaintext
plaintext = b"Welcome to the world of Camellia encryption! 🌐"

# Padding (16-byte block size অনুযায়ী)
padded_text = pad(plaintext, 16)

# এনক্রিপশন
ciphertext = cipher_encrypt.encrypt(padded_text)

print("🔑 Key (hex):", key.hex())
print("📡 IV (hex):", iv.hex())
print("🔐 Ciphertext (hex):", ciphertext.hex())

# -------- ডিক্রিপশন --------
cipher_decrypt = Camellia.new(key, Camellia.MODE_CBC, iv)
decrypted_padded = cipher_decrypt.decrypt(ciphertext)
decrypted_text = unpad(decrypted_padded, 16)

print("✅ Decrypted:", decrypted_text.decode())
```

---

### আউটপুট (উদাহরণ)

```
🔑 Key (hex): a1b2c3d4e5f67890123456789abcdef0123456789abcdef0123456789abcdef
📡 IV (hex): 1a2b3c4d5e6f7081920a1b2c3d4e5f60
🔐 Ciphertext (hex): 8d4f2e6a3b1c9d0f4e5a6b7c8d9e0f1a...
✅ Decrypted: Welcome to the world of Camellia encryption! 🌐
```

---

### ব্যাখ্যা

* **Key** → 16/24/32 bytes (128/192/256-bit)।
* **Block Size** → 16 bytes, তাই padding প্রয়োজন।
* **IV** → CBC mode এ প্রতিবার ভিন্ন ciphertext তৈরি করতে ব্যবহার হয়।
* **encrypt/decrypt** → একই key + IV দিয়ে উল্টো পথে কাজ করে।

---

Camellia হলো **AES-এর সমকক্ষ**, তাই এটি ব্যবহার করলে international standard ও high security উভয়ই পাওয়া যায়।

## 7. IDEA

### পরিচিতি

**IDEA** হলো একটি **সিমেট্রিক ব্লক সাইফার**, যা প্রথম তৈরি হয়েছিল **Xuejia Lai এবং James Massey** দ্বারা ১৯৯১ সালে।
IDEA মূলত **DES-এর দুর্বলতা দূর করার লক্ষ্য** নিয়ে তৈরি করা হয়েছিল।

IDEA আন্তর্জাতিকভাবে জনপ্রিয় হয়ে ওঠে বিশেষ করে **PGP (Pretty Good Privacy)** ফাইল এনক্রিপশনে।

---

### প্রধান বৈশিষ্ট্যসমূহ

1. **Block Size:** 64-bit
2. **Key Size:** 128-bit
3. **Rounds:** 8.5 rounds
4. **Structure:** Substitution-Permutation network (SPN)
5. **Symmetric Encryption:** Encrypt ও Decrypt একই key দিয়ে হয়
6. **Security:** DES-এর তুলনায় অনেক বেশি নিরাপদ

---

### IDEA এর কাজের ধাপ

IDEA মূলত **modular arithmetic** ব্যবহার করে। এটি তিনটি operation ব্যবহার করে:

1. **Addition modulo 2^16**
2. **Multiplication modulo 2^16+1**
3. **Bitwise XOR (exclusive OR)**

### Encryption Process

1. Input 64-bit block কে চারটি 16-bit অংশে ভাগ করা হয়: X1, X2, X3, X4
2. প্রতিটি রাউন্ডে (মোট 8.5 রাউন্ড):

   * Subkey যোগ করা হয়
   * Multiplication, addition এবং XOR করা হয়
   * Diffusion এবং non-linearity নিশ্চিত করা হয়
3. Last half-round (0.5 round) শেষে final transformation দিয়ে ciphertext তৈরি হয়

### Decryption

* Decryption একই প্রক্রিয়া উল্টোভাবে চলে
* Subkey গুলো reverse order এ ব্যবহার হয়
* Multiplicative inverse এবং additive inverse ব্যবহার করা হয়

---

### নিরাপত্তা

* IDEA এখন পর্যন্ত **practical attack থেকে নিরাপদ**
* Small block size (64-bit) এখন limitation, বড় ডেটার জন্য collision হতে পারে
* Brute-force attack প্রায় অসম্ভব (128-bit key)

---

### ব্যবহার ক্ষেত্র

* **PGP (Pretty Good Privacy)**
* Some secure messaging systems
* Legacy file encryption software

---

### উপসংহার

IDEA হলো DES-এর তুলনায় অনেক শক্তিশালী এবং দ্রুত block cipher।
তবে AES-এর উন্নতির কারণে আজকের দিনে IDEA অনেকটাই **legacy**।

👉 সংক্ষেপে:
**IDEA = 64-bit block, 128-bit key, 8.5 rounds, SPN, secure, primarily used in PGP।**

---

WebX-style takeaway ✨
**“IDEA ছিল DES-এর পরবর্তী প্রজন্মের হিরো, যা PGP-এর মাধ্যমে বিশ্বব্যাপী পরিচিতি পায়। যদিও আজ AES আধিপত্য বিস্তার করেছে, IDEA এখনও legacy security systems-এ গুরুত্বপূর্ণ।” 🔐**

## তুলনা টেবিল

আমি **বাংলায় একটি বিস্তারিত তুলনা টেবিল** তৈরি করেছি, যেখানে DES, 3DES, AES, Blowfish, Twofish, Camellia, এবং IDEA একসাথে তুলনা করা হয়েছে।

| ক্রিপ্টো অ্যালগরিদম     | ব্লক সাইজ | কী সাইজ         | রাউন্ড সংখ্যা | নিরাপত্তা               | গতি    | ব্যবহার / অবস্থা                                       |
| ----------------------- | --------- | --------------- | ------------- | ----------------------- | ------ | ------------------------------------------------------ |
| **DES**                 | 64-bit    | 56-bit          | 16            | নিরাপত্তাহীন (পুরনো)    | দ্রুত  | পুরনো লেগাসি, আজ নিরাপদ নয়                            |
| **3DES / TDEA**         | 64-bit    | 112/168-bit     | 48 (3×16)     | মাঝারি (লেগাসি)         | ধীর    | লেগাসি সিস্টেম, পুরনো ফাইল/ট্রানজেকশন এনক্রিপশন        |
| **AES-128 / 192 / 256** | 128-bit   | 128/192/256-bit | 10/12/14      | অত্যন্ত নিরাপদ (আধুনিক) | দ্রুত  | আধুনিক স্ট্যান্ডার্ড, HTTPS, VPN, disk encryption      |
| **Blowfish**            | 64-bit    | 32–448-bit      | 16            | নিরাপদ (পুরনো)          | দ্রুত  | legacy, password hashing (bcrypt), কিছু ফাইল এনক্রিপশন |
| **Twofish**             | 128-bit   | 128/192/256-bit | 16            | অত্যন্ত নিরাপদ          | দ্রুত  | Blowfish-এর উন্নত সংস্করণ, VeraCrypt, legacy systems   |
| **Camellia**            | 128-bit   | 128/192/256-bit | 18/24         | AES সমতুল্য নিরাপদ      | দ্রুত  | AES-এর সমমানের, এশিয়ায় জনপ্রিয়, TLS, VPN            |
| **IDEA**                | 64-bit    | 128-bit         | 8.5           | নিরাপদ (legacy)         | মাঝারি | PGP ফাইল এনক্রিপশন, লেগাসি                             |

---

✨ **সংক্ষেপে:**

* **AES এবং Camellia** আধুনিক স্ট্যান্ডার্ড এবং নিরাপদ।
* **DES, 3DES, Blowfish, IDEA** মূলত legacy এবং পুরনো সিস্টেমে ব্যবহৃত।
* **Twofish** হলো Blowfish-এর উন্নত এবং AES সমকক্ষ বিকল্প।

## ব্লক সাইফার কিভাবে কাজ করে?

- ডেটাকে নির্দিষ্ট সাইজের ব্লকে ভাগ করা হয়।
- প্রতিটি ব্লককে একটি গোপন কী দিয়ে এনক্রিপ্ট করা হয়।
- এনক্রিপ্টেড ব্লকগুলো একসাথে জোড়া লাগিয়ে পুরো ডেটা তৈরি হয়।
- ডিক্রিপ্ট করার সময় একই কী দিয়ে উল্টো প্রক্রিয়া চলে।

### সহজ উদাহরণ (Analogy)

ধরা যাক, আপনি একটি বড় বাক্সে ১০০টি চকলেট রেখেছেন।
- যদি কেউ বাক্সের তালা ভেঙে ফেলে, সব চকলেট পেয়ে যাবে।
- কিন্তু যদি প্রতিটি চকলেট আলাদাভাবে ছোট বাক্সে তালাবদ্ধ থাকে, তাহলে প্রতিটি চকলেট পেতে ১০০টি তালা ভাঙতে হবে!
- ব্লক সাইফার ঠিক এমনই—ডেটার প্রতিটি অংশ আলাদাভাবে নিরাপদ।

## কেন ব্লক সাইফার?

- বড় ডেটা এনক্রিপ্ট করতে সহজ ও নিরাপদ।
- বিভিন্ন মোড (ECB, CBC, CTR, GCM) ব্যবহার করে আরও নিরাপত্তা বাড়ানো যায়।
- দ্রুত এবং স্কেলেবল—ব্যাংক, ক্লাউড, মোবাইল সবখানে ব্যবহৃত।

## গুরুত্বপূর্ণ পয়েন্ট

- DES এখন নিরাপদ নয়, AES ব্যবহার করুন।
- কী-এর দৈর্ঘ্য যত বেশি, নিরাপত্তা তত বেশি।
- ব্লক সাইফার ছাড়া আধুনিক এনক্রিপশন অসম্ভব!

## অনুশীলন

১. আপনার কম্পিউটারে AES এনক্রিপশন লাইব্রেরি খুঁজুন (যেমন Python-এর cryptography, JavaScript-এর crypto)।
২. একটি ছোট টেক্সট ফাইল নিন এবং সেটি ব্লক সাইফার দিয়ে এনক্রিপ্ট করুন।
৩. কী পরিবর্তন করে দেখুন—ডিক্রিপ্ট হয় কি না?
৪. ECB ও CBC মোডের পার্থক্য খুঁজে বের করুন।

## উপসংহার

ব্লক সাইফার হলো নিরাপত্তার ভিত্তি। এটি ডেটাকে ছোট ছোট অংশে ভাগ করে প্রতিটি অংশকে আলাদাভাবে নিরাপদ রাখে। DES, AES, Blowfish, Twofish—সবই ব্লক সাইফারের উদাহরণ। আধুনিক এনক্রিপশন বুঝতে হলে ব্লক সাইফার ভালোভাবে জানা জরুরি।
